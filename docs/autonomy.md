# 自律作業の手引き — 誰にも訊けないときに、どう選び、どこで止まるか

**この文書は、オーナーに逐一確認せずに作業を進めるエージェント（[alteroid](https://github.com/takecchi/alteroid) など）のためのものである。**
人間が対話で作業しているときにも読んで害は無いが、**「訊けばよい」場面では訊くほうが早い。**

`AGENTS.md` は「どう書くか」（ADR・確かめていないことの明示・北極星への当て方）を述べている。
**この文書が足すのは「何を選ぶか」「どこで止まるか」「何をしてはいけないか」の3つだけである。**

---

## 1. 何を選ぶか

**上から順に見て、最初に当たったものを取る。**下の段は、上の段が空のときだけ見る。

| 順 | 取るもの | 空かどうかの調べ方 |
|---|---|---|
| **1** | **壊れているもの。**CI が赤い / 使う人に実害が届いている | `gh run list --branch main`、ADR の「引き受けた負債」のうち**使う人に届くもの** |
| **2** | **北極星の物差しを動かすもの。**「使う側が会話ログを全部積むのをやめられたか」に効く | `docs/roadmap.md` §3 の Phase 2 候補を、下の §1.1 の問いに当てる |
| **3** | **2 の前提になっているもの。**それ自体は物差しを動かさないが、動かす項目が乗れない | 同上。**「前提である」ことを ADR に書けないなら、それは前提ではない** |
| **4** | **測れていないものを測る。**主張は在るが実測が無い箇所 | ADR の「確かめていないこと」節を横断する |

**4段すべてが空なら、着手しない。**「やることが無い」と報告して止まるほうが、
**物差しを動かさない機能を足すより良い**——これは `docs/north-star.md` が
「機能の数でも recall の平均スコアでもない」と明示していることの運用版である。

### 1.1 段2 の当て方（この問いに答えられないものは段2ではない）

候補ひとつについて、次の3つを**着手前に**書けること。書けないなら段2ではない。

1. **これを入れると、何の数字が動くか。**（積む量の比 / `hit@k` / MRR / `omitted` の件数 …）
2. **その数字を、どうやって測るか。**既存の器（`examples/chat` の `retrieval` / `compare`）で測れるか、
   測れないなら**測る器を作るのが先ではないか。**
3. **動かなかったら、どうするか。**（落とす／別の形にする）

**⚠ 3 に「動かなかったら、それでも入れる」と書きたくなったら、それは段2ではない。**

### 1.2 同じものを二度やらないために

**着手前に必ず見る。**このリポジトリは複数のエージェントが並行して走ることがある。

```bash
git fetch origin main && git log --oneline -20 origin/main
gh pr list --state open --limit 20
```

**着手しようとしているものが、開いている PR と重なっていたら取らない。**次の候補へ移る。

---

## 2. どこで止まるか

**1つの PR は「1つの ADR とその実装」である。**

止まる条件（全部満たしたら PR を出して、**そこで止まる**）:

- [ ] ADR が在る（`AGENTS.md` の要件を満たす。**採らなかった案・引き受けた負債・これが覆るとしたら**まで）
- [ ] 6つの門が緑（`typecheck` / `lint` / `format:check` / `test` / `build` / `pack:check`）
- [ ] **歯が実際に噛むことを、変異試験で示した**（壊した入力で赤く、直したら緑に戻る）
- [ ] **CI が緑**（手元の `pnpm run test` は DB 段を実行しない。§4 を見ること）
- [ ] PR 本文に、測った数字と**確かめていないこと**が書いてある

**⛔ PR を出したら止まる。マージしない。**（§3）

**⚠ 「ついでに直す」をしない。**ADR に書いていない変更を混ぜると、
**その PR が何を主張しているのか読めなくなる。**別の PR にすること。

---

## 3. してはいけないこと

**次はオーナーだけが行う。**必要だと判断したら、**やらずに提起する**（ADR か PR 本文で）。

| ⛔ してはいけない | なぜ |
|---|---|
| **PR のマージ** | 取り込む判断はオーナーのもの |
| **npm への publish・Release の作成・npm 側の設定** | 取り消せない。`0.1.0` は上書きできない形で残っている（ADR 0066） |
| **`package.json` の `version` を上げる** | 版は Release の tag が決める（ADR 0070）。手で上げる意味が無い |
| **`docs/north-star.md` の書き換え** | 正典。方向そのものを変えるのはオーナーの判断（`AGENTS.md`） |
| **`docs/north-star.md` の「やらないこと」への追記** | オーナーが仕様に明示した非目標。`docs/vision.md` の「やらないこと」とは別物 |
| **公開 API の破壊的変更** | `0.x` なので semver 上は許されるが、**提起までにする。**ADR を書き、実装は別 PR にして、承認を待つ |
| **ライセンス・パッケージ名・依存の追加方針の変更** | ADR 0014 / 0061。製品の判断 |
| **`§5 級の判断`を自分で決めること** | 下の §3.1 |

### 3.1 判断が要るのに材料が無いとき

**推測で決めて進まない。**次のどちらかにする。

- **技術的に決められるなら決めて、理由を ADR に書く。**（`AGENTS.md`：
  「§5 に無いものは設計側で決めて理由を残す」）
- **製品・事業・安全性の判断なら、`docs/roadmap.md` §5 に項目を足す PR を出して、その作業は止める。**
  §5 は「オーナーの判断を集める場所」であり、**空になっても畳まないと明記されている。**

**見分け方**: 「どちらを選んでも技術的には成立するが、選び方が製品の性格を決める」なら §5 行きである。

---

## 4. 踏むと痛い穴（実際に踏まれたもの）

| 穴 | 何が起きるか | どうするか |
|---|---|---|
| **ADR 番号の衝突** | 並行 PR が同じ番号を取る。**1つのセッションで3回起きた実績がある** | **番号は PR を出す直前に取る。**`git fetch origin main` してから最大 +1。衝突したら README.md の1行と、本文・スクリプト・workflow の参照を全部付け替える |
| **`git checkout <file>` で変異を戻す** | **未コミットの編集も一緒に消える。**実際に3ファイル失われた（ADR 0066 測ったこと7） | 変異試験の前に**退避コピー**を取り、そこから戻す |
| **手元の `pnpm run test` が緑** | **DB 段を実行していない。**`DATABASE_URL` が無いと「実行していません」と告知して緑のまま通る（ADR 0015） | 出力を読むこと。DB 側は CI の3ジョブで見届ける |
| **手元の `pnpm run pack:check` が赤い** | `dist/` に古い `.map` が居残る（`tsc` は `outDir` を掃除しない）。**CI では起きない** | `rm -rf packages/*/dist && pnpm run build` |
| **擬似 provider の数字を「性能」と読む** | arm A（擬似埋め込み）の **MRR は 0.018**＝実質ランダム | 想起の質を測るなら `recorded`（ADR 0051）。`deterministic` は配線と契約の検査用 |
| **`npm view` で publish の成否を判断する** | registry の読み取り側は書き込みに数分遅れ、**CDN を迂回する `?write=true` でも 404 を返す**（ADR 0066 測ったこと8） | `npm publish` の出力で判断する |

---

## 5. 報告に必ず書くこと

**「できた」と「確かめた」を混ぜない。**PR 本文に次を分けて書く。

- **測ったこと**——コマンドと出力。**この器で実際に走らせたものだけ。**
- **確かめていないこと**——走らせられなかったもの（DB・実 API・workflow の本番経路など）と、その理由
- **人から受け取った前提**——自分で検証していないもの。**出所を書く**

**⚠ 他のエージェントの報告を、そのまま事実として引かない。**引くなら自分で検算するか、
「〜という報告を受け取った（未検証）」と書く。
