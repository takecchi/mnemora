# Architecture Decision Records

mnemora の設計判断のうち、**後から見て「なぜそうしたか」を追える形で残す必要があるもの**を
ADR (Architecture Decision Record) として記録する。`docs/architecture.md` や
`docs/memory-model.md` 等の他の docs が「何がどう決まっているか」を記述するのに対し、
ここでは各決定について、検討した選択肢・却下した理由・引き受ける負債・覆る条件までを
1ファイルにまとめる。**決定そのものをやり直す場ではなく、決定を記録する場である。**

alteroid (github.com/takecchi/alteroid) を根拠として引く箇所は、確認済み/未確認を分けた
一次調査の記録である [docs/alteroid-findings.md](../alteroid-findings.md) を参照する。

## 一覧

| 番号                                                      | 題                                                               | 状態           |
| --------------------------------------------------------- | ---------------------------------------------------------------- | -------------- |
| [0001](./0001-orm-drizzle.md)                             | ORM は Drizzle                                                   | 採用 (2026-09) |
| [0002](./0002-embedding-space-tables.md)                  | pgvector の抽象と埋め込み空間ごとのテーブル分割                  | 採用 (2026-09) |
| [0003](./0003-memorystore-vs-vectorstore.md)              | MemoryStore と VectorStore を分けるか                            | 採用 (2026-09) |
| [0004](./0004-decay-at-query-time.md)                     | 忘却をクエリ時に算出しつつ ANN 索引を殺さない                    | 採用 (2026-09) |
| [0005](./0005-job-queue-abstraction.md)                   | Job Queue の抽象                                                 | 採用 (2026-09) |
| [0006](./0006-memory-schema.md)                           | Memory schema の設計判断                                         | 採用 (2026-09) |
| [0007](./0007-tenant-scoping.md)                          | Tenant scoping                                                   | 採用 (2026-09) |
| [0008](./0008-absence-taxonomy.md)                        | 「無い」を分類して返す                                           | 採用 (2026-09) |
| [0009](./0009-usage-feedback-via-observe.md)              | 使用フィードバックを observe() で受ける                          | 採用 (2026-09) |
| [0010](./0010-decay-parameters.md)                        | 減衰の式とパラメータを固定する                                   | 採用 (2026-09) |
| [0011](./0011-no-window-count-in-ann-stage.md)            | 段1の ANN クエリに `count(*) OVER ()` を入れない                 | 採用 (2026-09) |
| [0012](./0012-ingest-pipeline-design.md)                  | 取り込みパイプライン（`observe()` / `runtime.tick()`）の実装方針 | 採用 (2026-09) |
| [0013](./0013-extraction-outcome-taxonomy.md)             | 抽出の失敗を、成功と同じ顔で記録しない                           | 採用 (2026-09) |
| [0014](./0014-package-name-mnemora.md)                    | 名前を `mnemora` / `@mnemora/*` に確定する                       | 採用 (2026-09) |
| [0015](./0015-root-test-gate-reports-skipped-db-tests.md) | ルートの `test` 門は、DB テストを「走らせなかった」と明示する    | 採用 (2026-09) |
| [0016](./0016-db-test-gate-explicit-exclusion.md)         | DB テストの排他は依存グラフに頼らず、門のコード自身に載せる      | 採用 (2026-09) |
| [0017](./0017-runmigrations-advisory-lock.md)             | `runMigrations()` を advisory lock でプロセス間排他する          | 採用 (2026-09) |
| [0018](./0018-register-embedding-space-advisory-lock.md)  | `registerEmbeddingSpace()` を advisory lock でプロセス間排他する | 採用 (2026-09) |
| [0019](./0019-real-openai-measurement-cost.md)             | 本物の OpenAI で北極星の物差しを測る — 費用・実測値・分かったこと | 採用 (2026-09) |
| [0020](./0020-temp-database-drain-before-drop.md)          | 使い捨てテスト DB は「接続0本」を実測してから `DROP DATABASE`（`WITH (FORCE)` を使わない） | 採用 (2026-09) |
| [0021](./0021-drain-embed-ticks-in-ingest.md)              | `examples/chat` の `ingestConversation` は `tick()` を干上がるまで回す | 採用 (2026-09) |
| [0022](./0022-fake-provider-compare-does-not-claim-recall-quality.md) | 北極星の「削っても目的の記憶が落ちない」を、擬似 provider の `compare` では主張しない | 採用 (2026-09) |
| [0023](./0023-subject-filter-in-ann-stage.md) | 段1の ANN クエリで `subject` を等値で絞る（`period` は降ろさない） | 採用 (2026-09) |
| [0024](./0024-remove-exact-counts-option.md) | 実装の無い `exactCounts` を、「予約」と書き残さずに削除する | 採用 (2026-09) |
| [0025](./0025-ann-underfill-is-not-reported-in-omitted.md) | 段1の ANN が窓を埋められなかったことが `omitted` に出ていない（実測のみ） | **未決** (2026-09) |
| [0026](./0026-ann-unreached-omission.md) | 近似索引が scope に届かなかったことを `Omission { kind: 'ann_unreached' }` として出す | 採用 (2026-09) |
| [0027](./0027-split-superseded-forgotten-omission.md) | `filtered` omission の `condition: 'status'` を `'superseded'` と `'forgotten'` に分ける | 採用 (2026-09) |
| [0028](./0028-reextract-superseded-cleanup.md) | `runtime.reextract` は古い抽出結果を `superseded` にする（`forgotten` にしない） | 採用 (2026-09) |
| [0029](./0029-reextract-skip-visibility.md) | `reextract` が既存 Memory を supersede しなかった理由を `ReextractResult.skipped` に出す | 採用 (2026-09) |
| [0030](./0030-update-status-compare-and-swap.md) | `MemoryStore.updateStatus` を compare-and-swap にし、`reextract` の安全弁の TOCTOU を塞ぐ | 採用 (2026-09) |
| [0031](./0031-supersede-status-and-event-in-one-transaction.md) | `reextract` の supersede の status 更新とイベント追記を、別々の2コミットから1トランザクションにまとめる | 採用 (2026-09) |
| [0032](./0032-outbox-claim-lease.md) | `OutboxStore.claimBatch` に claim のリースを足し、「見えない停止」と「先頭詰まり」を塞ぐ | 採用 (2026-09) |
| [0033](./0033-what-decided-the-rank-in-the-retrieval-bench.md) | `retrieval` ベンチが順位の理由を捨てていたのをやめる — 実際に順位を決めていた項の実測 | 採用 (2026-09) |
| [0034](./0034-vector-store-filter-conformance.md) | `VectorFilter` を「adapter が実際に適用しなければならない」契約にし、その契約を適合テストの歯として置く | 採用 (2026-09) |
| [0035](./0035-recalled-memory-provenance-kind.md) | `recall()` の返り値に `provenanceKind` を載せる — 「区別して返す」を返り値の側で満たす | 採用 (2026-09) |
| [0036](./0036-clamp-freshness-at-one.md) | `freshness` を 1 で頭打ちにする — 「まだ起きていない出来事は、最も古びていない」 | 採用 (2026-09) |
| [0037](./0037-callers-pass-occurred-at.md) | `observe()` の `occurredAt` を実際に通す — 「いつの出来事か」を絞れるようにする | 採用 (2026-09) |
| [0038](./0038-vector-hit-distance-is-cosine.md) | `VectorHit.distance` はコサイン距離だと契約に明記し、適合テストの歯で adapter 非依存に検査する | 採用 (2026-09) |
| [0039](./0039-period-boundary-conformance.md) | `period` の判定規則が4箇所に在ることを、境界の歯で固定する | 採用 (2026-09) |
| [0040](./0040-zero-vector-never-returned.md) | ゼロベクトルが絡む候補は `recall()` の結果に出ない — 契約は振る舞いで揃える | 採用 (2026-09) |
| [0041](./0041-reinforce-does-not-change-strength.md) | `reinforce` は `strength` を動かさない — 「強化」の意味を確定させる | 採用 (2026-09) |
| [0042](./0042-event-store-list-order-and-limit.md) | `EventStore.list` の並び順・`limit`・`since`/`until` を契約に明記し、適合テストの歯で固定する | 採用 (2026-09) |
| [0043](./0043-unit-assembly-dropped-omission.md) | 一対一の対向関係が破れて候補が単位から漏れたら、`omitted` に出す（黙らない） | 採用 (2026-09) |
| [0044](./0044-score-not-comparable-omission.md) | 段2の閾値比較を網羅的な三分割にし、`score_not_comparable` を `omitted` に出す | 採用 (2026-09) |
| [0045](./0045-budget-dropped-count-kind.md) | `budget_dropped` の `countKind` を、単位の網羅性から引き継ぐ | 採用 (2026-09) |
| [0046](./0046-contested-pair-invariant-tooth.md) | `contested` の一対一を「測れる形」にする——振る舞いは決めない | 採用 (2026-09) |
| [0047](./0047-fake-referential-integrity-existence-only.md) | 擬似物（in-memory 実装・core の Fake）にも外部キー相当の「存在」検査を適用する（「整合」までは広げない） | 採用 (2026-09) |
| [0048](./0048-reinforce-does-not-move-decay-origin-backwards.md) | `reinforce` は減衰の起点を巻き戻さない——比較を DB の1文へ入れる | 採用 (2026-09) |
| [0049](./0049-reinforce-monotonicity-in-pseudo-implementations.md) | `reinforce` の単調性を擬似物にも揃える——新しい決定ではなく、ADR 0048 の追随 | 採用 (2026-09) |
| [0050](./0050-tenant-event-retention.md) | `TenantSettingsStore` に event retention の読み書きを足す | 採用 (2026-09) |
| [0051](./0051-recorded-provider-cassette.md) | 記録した実 API の応答を再生する provider を、物差しの経路にだけ入れる（二層にする） | 採用 (2026-09) |
| [0052](./0052-compare-cassette-and-provenance-survival.md) | `compare` を実 API で測ったら「答えが落ちる」は消えた — 擬似物の産物だったことの確認と、その代償 | 採用 (2026-09) |
| [0053](./0053-set-embedding-status-does-not-roll-back-ready.md) | `setEmbeddingStatus` は `ready` を `failed` へ巻き戻さない（no-op。例外にしない） | 採用 (2026-09) |
| [0054](./0054-idempotent-create-from-the-insert-decision.md) | 擬似実装の `created` は挿入の決定そのものから出す——判定と挿入の間に `await` を挟まない | 採用 (2026-09) |
| [0055](./0055-extraction-prompt-subject-and-inference-not-added.md) | 抽出プロンプトに「主語を復元する1文」も「推論を生成する1文」も足さない——実 API 18 run で測った結果と、その代償 | 採用 (2026-09) |
| [0056](./0056-exclude-provenance-kinds-in-ann-stage.md) | 段1の ANN クエリで `excludeProvenanceKinds` を絞る（`period` は今回も降ろさない） | 採用 (2026-09) |
| [0057](./0057-dedicated-schema-namespace.md) | mnemora のオブジェクトを置くスキーマを、使う側が指定できるようにする — DML は `search_path`、DDL と存在検査は明示修飾 | 採用 (2026-09) |
| [0058](./0058-measure-the-time-term-in-a-separate-arm.md) | 時間項（`freshness` / `decay`）は、既存の probe set を書き換えずに別 arm で分離して測る — `probe-set.ts` に `occurredAt` を書き込まない | 採用 (2026-09) |
| [0059](./0059-period-in-ann-stage.md) | 段1の ANN クエリで `period` を絞る（`COALESCE(occurred_at, recorded_at)` の式索引を1本足す） | 採用 (2026-09) |
| [0060](./0060-publish-with-pnpm-four-packages-at-0-1-0.md) | npm へ出すのは4パッケージだけ・初回は `0.1.0`・梱包の道具は pnpm に統一する（ライセンスと `private` 解除は対象外） | 採用 (2026-09) |
| [0061](./0061-license-mit.md) | ライセンスを MIT にする — オーナーの決定、6パッケージの `license` と LICENSE の配布、tarball の側から検査する門（`private` 解除は対象外） | 採用 (2026-09) |
| [0062](./0062-contested-with-id-fk-index.md) | `memories.contested_with_id` の自己参照 FK に索引を足す — `tenant_id` 先頭の複合索引は RI チェックを効率良く供給できないこと・索引名は中身を保証しないこと | 採用 (2026-09) |
| [0063](./0063-hnsw-iterative-scan-not-adopted.md) | `hnsw.iterative_scan` は採らない — 件数は直るが正しさは直らない（`strict_order` は正しさを一切買わない）ことを3腕×4窓で測った記録 | 採用 (2026-09) |
| [0064](./0064-exact-path-cost-vs-scale.md) | 厳密経路の費用倍率を規模で振って測った — ADR 0063 の「約2.5倍」の訂正と、`relaxed_order` の recall が規模とともに悪化するという発見 | 採用 (2026-09) |
| [0065](./0065-vector-store-space-separation-conformance.md) | `VectorStore` の space 分離を適合テストの歯にする — `FakeVectorStore` に丸ごと空いていた一段と、監査の漏れの記録 | 採用 (2026-09) |
| [0066](./0066-start-publishing-with-oidc.md) | **publish を始める**（対象4つの `private` を外す）— 梱包は pnpm・アップロードは npm（Trusted Publishing / OIDC）、引き金は GitHub Release。梱包の欠陥4件（`@types/pg` / `workspace:^` / `exports` / `vitest` の peer 化）を初版の前に直した。**`0.1.0` は publish 済み**（初版は OIDC で出せないため token 経路。provenance 無し） | 採用 (2026-09) |
| [0067](./0067-dry-run-fail-open-and-does-not-verify-trusted-publisher.md) | 予行フラグ (`dry_run`) の fail-open を安全側へ反転する — `--dry-run` は信頼発行元 (Trusted Publisher) の未設定を検出できないと実測した記録 | 採用 (2026-09) |
| [0068](./0068-the-bench-must-not-lie-about-what-it-measured.md) | `retrieval` ベンチが「測っていないこと」を測ったかのように印字するのをやめる — 2回目の実行で `ingest` が逆の結論を出すこと・出力から arm を跨いで数字を拾えること・`OPENAI_API_KEY` が在るだけでカセット再生が実 API へ倒れること | 採用 (2026-09) |
| [0069](./0069-ann-truncated-says-nothing-about-loss.md) | `ann_truncated` は「損したか」を一切言っていない（スコープが `k'` 以上なら必ず鳴る）— 読み手0件・書かれた一手は 100% 発火・`k'=40` の安全性は「4項が定数」に依存しており `occurredAt` を入れると割れることの実測と、案A（安全余裕を札に載せる）の採用 | 採用 (2026-09) |
| [0070](./0070-version-comes-from-the-release-tag.md) | **版の権威を Release の tag に置く** — `vX.Y.Z` の Release が `package.json` の `version` を書き換えてから梱包する（版上げのコミットが要らなくなる）。`package.json` の値は権威ではなくなる。併せて OIDC と provenance が実際に通ったことの記録 | 採用 (2026-09) |
| [0071](./0071-delegate-phase-2-selection-to-autonomous-agents.md) | **Phase 2 の作業選定を自律エージェントへ委譲する** — 何を選ぶかの4段の優先順・境界は「取り消せるか」で引く・訊けないときは §5 へ足して止まる。実際に踏まれた穴6件を `docs/autonomy.md` に集めた | 採用 (2026-09) |
| [0072](./0072-anthropic-llm-provider.md) | **`@mnemora/anthropic` を足す** — `LLMProvider` だけを実装（Anthropic に埋め込み API は無い。`docs/architecture.md` §4 が既にそう設計していた）。翻訳先は強制 tool use ではなく**ネイティブの構造化出力**（`output_config.format`）。`.optional()` が optional のまま残るため OpenAI 側の nullable 化と `stripNulls` の往復が要らない。provider の適合テストは testkit に無く、両 provider を並べる歯を1本置いた。**追記(2026-09-09)**: 初版は `content` を読む前に `stop_reason` を見ておらず、拒否（HTTP 200 で返る）と空応答が同じ例外になっていた — 決定6 で `kind` により区別する | 採用 (2026-09) |
| [0073](./0073-digest-band-bounded-without-taxonomy.md) | **digest 帯を実装する** — 「digest 帯は taxonomy を要する」は理由の記述の誤りだった（`DigestEntry` は `axis` を持たず、`digest` は Phase 1 の `NOT NULL` 列）。帯が並べるのは**返さなかったもの**。上限は件数 (50) ・帯全体の文字数 (4000) ・1件の長さ (120) の**3つ**——`digest` に長さの上限が無いため件数だけでは閉じない。どの上限で切れたかは `digestBandCoverage.limitedBy`。⛔ Phase の線は動かしていない（この1機能だけの前倒し） | 採用 (2026-09) |
| [0074](./0074-impression-topic-growth-what-mnemora-cannot-hold.md) | **「印象」「話題」「成長」を mnemora は持てるか** — 現物で当てた結果と、3つの案。**決定していない**（3案を並べ、選ばなかった記録）。選ぶのはオーナーであり、末尾の「オーナーへ差し戻す問い」がその入口 | **提案 (2026-09)** |
| [0075](./0075-openai-refusal-and-truncation.md) | **`@mnemora/openai` も拒否・打ち切りを「空の成功」にしない** — ADR 0072 の負債6 の返済。OpenAI は `stop_reason` を持たず、`message.refusal`（拒否の文面。このとき `content` は `null`）と `finish_reason`（`length` / `content_filter`）の**2機構**を `content` を読む前に見る。`content_filter` は `kind` を割らず `refusal` に含め、生の `finishReason` で情報を残す | 採用 (2026-09) |
| [0076](./0076-extraction-carries-failure-kind.md) | **抽出は例外を飲み続けるが、中身は捨てない** — ADR 0013 が却下したのは「例外を伝播させる」ことであって「中身を記録する」ことではない。`kind` を `ObserveResult` / `ReextractResult` / `memory_events.meta.failureKind` まで運ぶ。**`ExtractionOutcome` の3値は変えない**（分岐に網羅性チェックが無く、値を足すと黙って既定側へ落ちる）。欄は必須にして全構築経路（7本）を型検査に列挙させた | 採用 (2026-09) |
| [0077](./0077-testkit-fixtures-subpath.md) | **インメモリのストアを `@mnemora/testkit/fixtures` から出す** — 本物の provider から `ObserveResult` までを通す歯にはストアが要り、**実測で3メソッドの戻り値が使われる**ため「全部 throw する偽物」では通らない。⛔ **`index` からは出さない**——プレースホルダを `createStore` に渡すと自分の実装を1文字も測らずに緑になるため。危険は消えていない、隔離しただけである | 採用 (2026-09) |
| [0078](./0078-strength-value-range.md) | **`Memory.strength` の値域を `(0, 1]` に塞ぐ** — 口を開ける前に蓋を付ける（ADR 0036 が `occurredAt` の前に `freshness` の上限を入れたのと同じ順序）。`strength` は ADR 0041 が「初期値として設定できる欄」と決めた欄なのに、zod にも DB にも制約が無く `NaN` / `Infinity` / 負 / `1e6` が素通りしていた。**強制の責任は store の層**——`MemorySchema` は `.parse()` される箇所が0件なので zod は防波堤ではない（締めたのは型の契約として）。生 SQL の一括投入経路は DB の CHECK だけが守る。⚠ 変異試験で `Number.isFinite` に歯が当たらないことが分かり（冗長だった）、コードのほうを消した。⛔ **`Runtime` から初期値を設定する口は開けない** | 採用 (2026-09) |
| [0079](./0079-requeue-embed-jobs.md) | **索引に載らなかった Memory を積み直す口を開ける** — 診断は「静かに漏れる」ではなく**「見えているのに直せない」**。`recall` は `not_indexed` として件数を既に正しく名乗るが、[docs/recall.md](../recall.md) §4 が案内する次の一手（`pending` は待つ・再試行する / `failed` はパイプラインを疑う）を**実行する口が公開 API に無い**——`fail` は終端で自動リトライも無い（ADR 0032）ため、provider が直っても永久に索引へ戻らない。`MemoryStore.requeueEmbedJobs` と `Runtime.reembed` を足し、**列挙と積み直しを1つの口が兼ねる**（件数しか見えないので、id を得る手段が別に要る）。🔴 **`fail` の終端性は変えない**——積み直しは新しい outbox 行であり、ADR 0032 を覆さない。⚠ `pending` も対象に取れるのは、`processEmbedJob` の `try` の外の2経路で**待っても解けない `pending`** が作られうるため（**構造から読める「起こりうる」であって、発生は観測していない**）。⛔ 案B（`fail` を非終端にするリトライ）は**いま採らない**（`maxAttempts`/`backoff` が `leaseMs` と同じ形で破壊的変更になる。永久に採らない、ではない） | 採用 (2026-09) |
| [0081](./0081-similarity-is-the-only-term-that-ranks.md) | **順位を決めているのは `similarity` ただ1項である** — ADR 0033 は項ごとの「幅」を出していたが、幅は**何通りの値を取ったか**を答えない。候補集合の上で `tagMatch` と `strength` は**1通り**（厳密に 1）、`freshness` は `decay` の**行ごと厳密な複製**（10件×7probe×3arm で不一致0件。ADR 0033 の間接証拠を直接証拠へ格上げ）、`decay` は10通りだが変域が **1e-8 桁**。⟹ **`(a)` は2つに割れる**——`(a-1)`（重みを回せば直る）は **0件**、`(a-2)`（スコアに入れる値が無い）が1件（travel）。**⟹ 重みを触る道は好みではなく構造として閉じている。**`(b)` は2件（exercise / diet）、`(c)` は **0件**（75件すべてに埋め込みが1対1、outbox に pending も failed も0）。⚠ **ADR 0033 §4「`occurredAt` を渡す箇所は0件」を訂正する**（ADR 0037 / #39 で口は開いており11箇所。**ベンチが通していないだけ**）。⚠ arm C は **0.738**（ADR 0033 の 0.750 とは一致しない。0.714 は arm B）。⛔ **実装を含まない**——ベンチの印字が `decay`/`freshness` の差を隠す欠陥は**意図的に残している** | 採用 (2026-09) |
| [0082](./0082-tick-names-unsupported-job-kinds.md) | **`tick` が処理できない kind を、`failed` の中に埋めない** — issue #105（初の外部フィードバック）の**推測は現物では起きていなかった**（走らせて確認: `kinds: ["consolidate"]` は claim され `fail()` で終端に落ち `{processed:0,failed:1}` を返す。lease 切れの再 claim も無い。この throw は Phase 1 最初期から在る）。残っていた欠陥は別で、**「provider が落ちて失敗した」と「その kind を処理できない」が `failed: 1` という同じ顔になる**こと——ADR 0029 が `reextract` で塞いだのと同じ族（ADR 0008）。`TickResult.unsupported` に**名指しで**出す（`failed` の意味は変えない。内訳である）。根は「tick が何を処理するか」が**3か所に写されていた**ことなので、`TICK_SUPPORTED_JOB_KINDS` を唯一の出所にし `Record<TickSupportedJobKind, JobHandler>` で**型検査に結んだ**（どちらの向きのずれも `tsc` が止めることを実測）。⚠ **JSDoc に一覧を数え直さない**——`consolidate`/`reflect` の本体は入る予定であり、散文の写しは黙って嘘になる。時限的な主張は**本体が入ったら赤くなる歯**として置いた。⛔ 案C（claim 前に throw）は**公開 API の破壊的変更**なので提起にとどめる（autonomy.md §3） | 採用 (2026-09) |
| [0083](./0083-cjk-aware-heuristic-token-counter.md) | **既定 `TokenCounter` を文字種で重み付けする** — issue #108（2件目の外部フィードバック）。`Math.ceil(text.length / 4)` は**日本語の合計を実測の 0.353 倍しか数えず**（中国語 0.308 / 韓国語 0.373）、`RecallBudget.maxMemoryTokens` はこの値で判定されるので**予算が静かに超過していた**。⚠ **報告者の係数（CJK 0.7）は使わない**——自分で測ったら `ja` の合計比 0.92（まだ8%過小評価）で1件ごとの最悪は −21.4% だった。js-tiktoken の `o200k_base` で121件を実測し（**実 API と 10/10 一致することを先に確かめた**。26回・約 $0.00009）、**CJK 0.9 / 非CJK 0.25（据え置き）・コードポイント基準・20分率の整数比**に決めた。0.9 は (1) 回帰 0.8885 を小数第1位に丸めた値 (2) 全121件の合計比が最も 1.00 に近い（**0.991**） (3) `ja` 16件を1件も過小評価しない最小の候補、の3つが同じ値を指した。**`counter: 'heuristic'` は維持**（精度が上がったから `exact` と名乗ってよい、と読まれるのがいちばん危ない）。⛔ **依存は増やさない**（autonomy §3）——実測はカセット式の fixture として持ち込み、再生成手順は門の外に置いた。⚠ **直っていない範囲を歯で固定した**（タイ語 0.564 / アラビア語 0.813 / キリル 0.864 は過小評価のまま。`cl100k_base` では日本語を約12%過小評価）。**穴を1つ塞いだ**——`maxMemoryTokens` / `promptBudgetTokens` を行使する歯は変更前 **0本**だった | 採用 (2026-09) |
| [0084](./0084-lexical-recall-channel.md) | **recall に語彙候補生成チャンネルを足す** — Issue #106（初の外部からの実利用フィードバック）。候補生成が ANN 1本しか無く、固有名詞・識別子を**文字列として**引く経路が無かった。`RecallQuery.channels` と `LexicalStore` を足す。**⛔ `REQUIRED_EXTENSIONS` を増やさない**——`pg_trgm` が買えるのは「日本語の固有名詞」1点なのに、**`C` ロケールのクラスタでは日本語のトライグラムが空になり黙って0件になる**（実測）。組み込みの `to_tsvector('simple', …)` は**素のままでは日本語文中の識別子を引けない**（実測 `f`）が、ASCII 境界の正規化を掛けると引け、`PROJ-5678`/`TASK-1234` を誤爆しない。⚠ **報告者の主張（同形式の別 ID に埋もれる）は実埋め込みでは単純な形で再現しなかった**——埋もれたのは「希釈 × 表記ゆれ」等の複合条件のときだけ（§9）。⚠ **日本語の文に埋もれた日本語の語（人名）は引けない**（§8 の負債）。⛔ `"recent"` はユニオンに入れない（実装を伴わない値は `retrievedVia` の `tag_match`/`recency` と同じ欠陥を新規に作る） | 採用 (2026-09) |
| [0085](./0085-local-embedding-provider.md) | **外部サービスに繋がない埋め込み provider を足す** — 【オーナー】の決定（この用途で API キーを要求する設計は採れない。⚠ ただし `packages/openai` は削らない・既定から外さない。**足す**決定である）。ADR 0081 が「順位を決めるのは `similarity` ただ1項」、ADR 0019 §7.4 が「動かしていたのは埋め込みであり抽出ではない」と測っている以上、**埋め込みの質が想起の質をほぼ決める。**⚠ **前提を1つ訂正した**——「鍵が無いから CI で測れない」は**もう真ではない**（カセットで実 API 由来の152件を鍵なしに再生でき、ADR 0081 自身がその経路で測っている）。**本当の論拠はカセットの構造的な限界**（鍵は入力の SHA-256 なので**probe を1件足すと鍵が変わる** ⟹ ゴールデンセットを育てる道が閉じている。現に probe は**7件しかない**）。🔴 **どのモデルも否定・時制・矛盾を解けない**（「コーヒーより紅茶が好き」と「紅茶よりコーヒーが好き」の cos が **0.996**）⟹ ⛔ **「埋め込みを替えたから精度が上がった」と外に書かない。**採ったのは `sirasagi62/ruri-v3-30m-ONNX` q8（256次元 / 重み36MB / 4スレッドで985文/秒 ⟹ 100万件20分 / apache-2.0）。**⛔ `EmbeddingProvider` の契約を変えない**——`embed()` の本番の呼び出し口は `recall-runtime.ts` と `runtime.ts` の**ちょうど2つ**で、クエリと文書を区別できない。**対称 prefix で有意に悪くならない**ことを測り（ruri ΔMRR −0.031, CI [−0.094,+0.031]／e5-small は **32本中0本しかランキングが変わらず ΔMRR ちょうど 0.0000**）、**非対称を表現できない型**にした。🔴 **`space.model` に方式を焼く（`"ruri-v3-30m/sym"`）**——後で非対称へ移ったとき**既存ベクトルと新ベクトルが同じ空間に混ざる**のを、ADR 0002 のテーブル分割に乗せて構造で防ぐ。遅延ロードは **Promise を握る**（素朴な実装が**8重ロード**して 557ms/406MB が 3,138ms/1,009MB になる実測がある）。⛔ **`allowBuilds` は `onnxruntime-node: false`**（CPU バイナリは同梱済みで postinstall は **CUDA EP 302MB** を足すだけ。拒否のほうが軽く、かつ「GPU 基盤の構築」の線を踏まない）。**【現物】migration は要らなかった**——ベクトル列を持つ migration は0本で、`registerEmbeddingSpace()` が実行時に `vector(N)` を作る（ADR 0002 の配当）。⚠ `Xenova/multilingual-e5-*` は**ライセンスを3点すべてに表明していない**ので却下（埋め込みは intfloat 版と全桁一致）。⛔ **#109 を塞がない**（ゴールデンセットの拡張と CI 配線は別の作業）。負債: 個人の変換 repo への依存（**承知のうえの選択**。再変換手順を README に残した）・`node_modules` +383MB・重みは実行時 DL・**`EmbeddingProvider` の適合テストは今も無い**（ADR 0072 の覆る条件に該当 ⟹ [#116](https://github.com/takecchi/mnemora/issues/116)） | 採用 (2026-09) |
| [0086](./0086-no-import-meta-in-published-artifacts.md) | **配布物から `import.meta` を消す** — 外部の採用検討者からの初の実利用報告（#110）。⚠ **報告の指摘ファイルは誤りで、実体は `src/migrate.ts`**（`index.ts` が再 export するので**入口そのもの**が壊れる。報告より広い）。🔴 **「関数の中へ遅延させる」では直らない**——`import.meta` は early error で、**呼ばなくても読み込んだ時点で落ちる**（素の Node と `transpileModule`+V8 の両方で実測。**当初オーナーが推していた案を実測が否定した**）。⟹ 自分の位置を知る処理だけを `migrations-dir.cts`（`__dirname` は構文ではなくただの変数）へ追い出す。**`exports` も `"type": "module"` も変えないので公開契約は同一**——デュアルビルドではない。⛔ `eval("import.meta.url")` は**大きな声で落ちるものを静かに落ちるものに変える**ので却下。門は `dist` を CJS へ変換して**構文解析させる**（🔴 `grep` だとこの修正自身のコメントに在る `import.meta` 7回を誤検知して赤くなる）。#107（CLI の `--schema` / `MNEMORA_SCHEMA`）は **ADR 0057 の「受け口は3つ」に CLI が入っていなかった取りこぼし**であり新しい決定ではない（優先順位「引数 > 環境変数」だけがここで新しい） | 採用 (2026-09) |
| [0087](./0087-runtime-forget-shape.md) | **`Runtime.forget()` の形を決める** — Issue #102（THE PHAGE の社内 Slack エージェントへの導入検討で見つかった外部フィードバック）。⚠ **「forget を実装するか」はこの ADR では決めていない**——`docs/vision.md` の5動詞・`docs/memory-model.md`「Phase 1 は `forget()` のみを実装する」・オーナー回答（`docs/roadmap.md` §5.3、2026-09-06）で既決。**`docs/north-star.md`「やらないこと」10項目にも `docs/roadmap.md` §1.3 の除外5項目にも忘却は無い**（正典は書き換えていない）。無かったのは公開 API の口だけで、ADR 0030 が既に「まだ実装されていない」と名指ししていた。⟹ 決めたのは**形**である。🔴 **返り値は対象ごとに6値**（`forgotten` / `already_forgotten` / `not_found` / `conflicted` / `failed` / `not_attempted`）——「忘れた」「もともと無かった」「見ていない」を潰さない（ADR 0008 の判定基準）。⛔ **`MemoryStatus` に値を足さない**（`forgotten` は既に在り、理由の粒度は `memory_events.meta` へ落とすのが §9 の規律。status を割ると status ゲート3か所が独立にずれる＝ADR 0082 の根）。冪等性は `expectedStatus` の compare-and-swap（ADR 0030 と同じ道具）で買い、**再試行のループは回さない**（読み直し1回で打ち切り `conflicted` と名乗る）。複数対象の原子性は**買わない**（ADR 0031 が明示的に買わなかったもの）——予期しない例外は打ち切って**投げずに返す**（投げると、既に忘れて監査ログに残った分が呼び出し側から見えなくなる）。⛔ **`recall` は1行も変えていない**（status ゲートが既に `["active","contested"]`、`filtered/forgotten` の札も ADR 0027 で既に在る）——推論ではなく歯で測った。⛔ 本番コードにテスト用の seam を足さない（並行の歯は fake 側で作る。ADR 0077）。⚠ **変異試験で偽物のほうの欠陥が出た**——`FakeMemoryStore` は `Memory` をその場で書き換え `getMany` が**同じ参照**を配るので、「読んだ後に別の誰かが書き換えた」を前提にする歯は**素の偽物の上では原理的に測定不能**（落ちる向きは常に緑）。歯の側で複製を挟んで初めて赤くなった。ついでにコードのコメントの主張（写しを更新しないと2回とも `forgotten` になる）が**嘘**だと分かった——CAS が救うので `outcomes` は変わらない。買っているのは往復であり、コメントを事実に直した。⚠ 負債: `contested` を forget すると対向の一対一が破れる（**Phase 1 に `contested` を書く経路は0本**）／埋め込みは索引に残る（`purge()` の担当）／`forgotten` → `active` の復元口は無い | 採用 (2026-09) |
| [0088](./0088-retrieval-quality-measured-in-ci.md) | **想起の質を CI で継続計測する — 記録の再生で鍵なしに測り、⛔ 門にはしない** — Issue #109（3件目の外部フィードバック）。🔴 **issue が立つ前提の「API キーが必要」は既に偽だった**——ADR 0051 のカセット（実 API の埋め込み **152 件**・`text-embedding-3-small`/256 次元）を `MNEMORA_PROVIDER_SOURCE=recorded` で再生すれば鍵なしに測れる（ADR 0081 自身がこの経路で測っている）。⟹ **週次・secret ではなく毎 PR・鍵なし**で塞げる。塞がっていたのは「誰も CI に配線していなかった」ことのほうで、`.github/**` の `retrieval` 言及は **0 件**だった。⭐ これは **ADR 0022 の「これが覆るとしたら」第1項**が名指しした負債である（記録の再生は追加コスト 0 ＝ 文言どおりの「安価な embedding provider」）。⛔ **門にしない**——【実測】2 回の実行でまとめ表は完全一致したが**出力全体は 116 行違い、`decay`/`freshness` が時刻依存で揺れて `total` の6桁目が動く**。見出しが安定するのは `similarity` の変域が `decay` の約 10⁶ 倍だからで（ADR 0081 §1 の再現）、**「2 回一致」は決定性の証明ではない**⟹ 偽陽性率に上限を置けない門は無いより悪い。品質の門も置かない（**標本 probe 7 件**、ADR 0033 §3）。⚠ **値を残すだけでは読まれない**——`ci.yml` の `compare` が既に「ログから読めるようにする」と約束したきり assertion を1つも持たない先例がある。⟹ **基準値を repo にコミットし（値が動けば PR の diff に出る）**、差分を **Job Summary**（生ログではない）に出し、**一致時は1行で黙る**。🔴 機械可読出力は**数字と条件（実際の `llmMode`/`embeddingMode`）を同じ構造から離さない**——0.714(arm B) と hit@10 7/7(arm C) の合成は**3 度起きている**（⭐ 今回 4 度目が起きなかったのは委譲文が毎回警告していたからで、仕組みではない）。⛔ **名乗れるのは「検知できるようにした」までで「検知する歯にした」ではない**——閾値の門には数十件の標本が要り、それは**カセットでは構造的に閉じている**（probe を1件足すと鍵が変わり例外）| 採用 (2026-09) |
| [0089](./0089-runtime-consolidate-shape.md) | **`Runtime.consolidate()` の形を決める** — Issue #103（#102 と同じ外部の採用検討者。「圧縮の主体が呼び出し側と mnemora に二重化する」）。⚠ **Issue 本文の「`CLAUDE.md` / `AGENTS.md` にも Phase 1 の範囲外と書かれている」は現物では事実ではなかった**（両ファイルに `consolidate` の語は**0件**。`roadmap.md` §1.3 の除外5項目にも Phase 2/3/4 の表にも無く、`vision.md` は5動詞の1つとして挙げている）⟹ **範囲内。正典は書き換えていない。**ADR 0087 と同じ手続きで「その決定は既に在るか」を先に問い、答えは「**意味論は既に在る、形は無い**」だった。🔴 **芯は「統合元をどの層で始末するか」**——減衰でも `forget()` でも `purge()` でもない**第4の位置＝`superseded`**（`memory-model.md` §11 行5 が「統合パイプライン」を名指しでこの遷移に置く。north-star 表 行4「元の Memory を捨てる」は**落ちた案**であり行も `content` も残す）。`forgotten` は絶対に統合元にしない。「無い」は**2階層**で割る（全体の結末5値 ＋ 対象ごと7値。語彙は `ForgetOutcome`/`ReextractSkip` から借り**新しい綴りを作っていない**）——特に `nothing_to_consolidate`（探して無かった）と `not_examined`（探していない）、`no_eligible_sources` と `single_eligible_source` を潰さない。**冪等性は「読んで status で弾く」順序が買っている**（2回目は eligible が0件になり **LLM を呼ばず**何も書かない。専用の冪等キーも索引もマイグレーションも足していない）。`dryRun` は **LLM を呼ばない**（北極星の問い5）。`MemoryEventKind` に値を足さず `meta.reason='consolidated'` で割る ⟹ **ADR 0074 が予言した「2つ目の書き手」がこれであり**、`superseded` を「より良い抽出に置き換えられた」と説明していた doc コメント4箇所を直した。⛔ **`tick` は触らない**（Issue 本文が「別途 issue を立てます」と明示）——ADR 0082 の時限式の歯は**緑のままで主張も正しい**（tick に分岐は無い）が、「本体」が*動詞*と*tick 分岐*の2つを指すため doc に1文だけ足した（**アサーションは1文字も変えていない**）。⚠ **`memory-model.md` §11 行5 の「1トランザクション」は満たしていない**——道具が無く（`updateStatusWithEvent` の doc 自身がこの欠落を名指ししている）、**`reextract` が既に同じ形**であり未達に2人目の呼び手を足しただけである（統合先を先に作り、途中で落ちても失われるものが無いようにした） | 採用 (2026-09) |
| [0090](./0090-embedding-input-token-limit.md) | **埋め込みの入力が上限を超えたことを名乗る — 8192 トークンの壁と、導かれる識別子の余裕** — 誰も測っていなかった2点を測り、**渡された前提が2つとも現物では成り立たなかった。**🔴 ①【受領】「別モデルで日本語 800〜1200字で黙って切られる」は **`ruri-v3-30m` では再現しない**——壁は **8192 トークン**（`tokenizer_config.json` の `model_max_length` / `config.json` の `max_position_embeddings` をこの器から取得。【実測・委】cos が 1.0 に張り付き始めるのは **18,442 字**から＝ 8193 トークン。**普遍的なのはトークン数だけで、字数はトークン/文字比に依存する**）。⟹ ⭐ **8192 > 8191 なので「OpenAI より上限が狭い」は成立しない。**差は**上限に当たったときの振る舞い**のほうである（`local` は黙って切って正常な顔のベクトルを返す。⚠ OpenAI 側がサーバで拒否することは**受領のままで裏を取っていない**）。切っているのは実装ではなく **`@huggingface/transformers@4.2.0` の呼び方**（`feature-extraction.js:89-92` が `max_length` **無し**で `truncation: true` を渡し、`tokenization_utils.js:405/428/438` が `model_max_length` に丸めて `truncateHelper` が `item[key].length = length` で破壊的に切る。**戻り値もログも警告も無い**。切るのは `> max_length` のときで⭐**上限ちょうどは切られない**）。🔴 **【受領】「いまは噛まない見込み」はカセットについては真、コードについては偽**——カセットは平均22.36字・最長38字で壁から2桁下だが（`retrieval.json`。⚠ `compare.json` は別分布で平均15.49字＝受領値は前者限定だった）、**`observe()` の生テキストが無加工で `embed()` へ届く経路が在る**（`observation.ts:122-131` の `content` は `.max()` 無し → `extraction.ts:206-217` の LLM 失敗 `catch` → `fallbackWholeObservationCandidate` → `observationPayloadText`（無加工）→ `runtime.ts:1011`）。`consolidate`（ADR 0089）も上限を持たず、統合結果が再び対象に入りうるので**縮む保証がコードに無い**（⚠ 単調増加は未測定）。⟹ 決定: **`kind: "input_too_long"` の例外にする**（ADR 0075 が LLM 側で決めた「打ち切りを空の成功にしない」と同じ固定点。`embed()` の戻りは `number[][]` で `Omission` の余地が型に無く、そこを作るのは `EmbeddingProvider` の契約変更＝ADR 0085 決定2 の線を崩す）。**検査は推論の前**・境界は **`>`**。⟹ `processEmbedJob`（`runtime.ts:1017-1020`）が `embeddingStatus: 'failed'` を書くので、**名前の付いた状態に着地する**——黙って切ると残るのは「前 8192 トークンだけを表す、正しい顔をしたベクトル」で原因を追えない。⭐ **`Infinity` を「上限が無い」と読まない**（transformers.js は宣言が無いとき `Infinity` を返す）⟹ `kind: "unknown_input_limit"` で組み立て自体を失敗させ、逃げ道は既存の `createPipeline` 注入に任せる。🔴 **検査を純関数 `buildLocalEmbeddingPipeline(extractor)` に切り出した**——トークナイザは本物のモデルを落とさないと触れない場所に居るので、そこに検査を書くと**歯にならない**（`pipeline.ts` のヘッダ自身が「ユニットテストで測っていない」と書いていた）。⛔ **公開 API は壊さない**——より強い案（`LocalEmbeddingPipeline` を `{maxInputTokens, countTokens, embed}` の必須 interface にする）は `docs/autonomy.md` §3 の ⛔ 項目なので**実装せず提起する**（負債1）。任意プロパティ案は却下（「上限が無い」と「宣言を忘れた」を潰す）。`TokenCounter` での代用も却下（**誤検出率を測れない門は置かない**）。chunking も却下（`texts` と件数が合わなくなる／probe 7件では測れない）。⭐ ②【実測】識別子は **63B 以内。改名は要らない**（テーブル `memory_embeddings_local_ruri_v3_30m_sym_256` = **43B**、HNSW 索引 = **52B**）。🔴 **締め付けているのは索引名のほう**——接頭辞が `idx_memory_embeddings_hnsw_`（27B）対 `memory_embeddings_`（18B）で**常に 9B 長い**⟹ **テーブル名だけ見る歯は索引名が化けた状態を緑で通す**（受領した2例はテーブル名だけで語られていた）。🔴 そして **「63B 以内」はそのままでは歯にならない**——導出関数は超過分を切り詰めて**必ず 63B 以内に収める**ので事後条件そのもの＝**永久に緑の嘘の歯**。⟹ 固定するのは**名前が化けていないこと**で、切り詰め形は末尾の `dimensions` を捨てて `_<sha256 8桁>` を足すため **`endsWith(`_${dimensions}`)`** で検出でき、それが赤くなれることを長すぎる `model` で実際に化かす歯で裏打ちした。期待値 `63` は**逐語**（⛔ `MAX_IDENTIFIER_BYTES` から組み立てると定数を変える変異が自己整合して素通りする）。歯は `packages/postgres` ではなく `packages/local-embedding` 側に置いた（前者は `test` script を持たず `pnpm -r run test` で走らないため）。⚠ 負債: **注入された pipeline には検査が効かない**（負債1 で構造的に消せる）／**符号化が2回走る**／**`observe()` の入力に上限は無いまま**（本 ADR は「当たったことが分かる」までしか買っていない）／`packages/openai` は1行も触っていない（そちらにも入力長の検査は無いが、同じ穴かは未確認）／**`EmbeddingProvider` の適合テストは今も無い**（ADR 0085 負債4 / [#116](https://github.com/takecchi/mnemora/issues/116)） | 採用 (2026-09) |
| [0091](./0091-runtime-reflect-shape.md) | **`Runtime.reflect()` の形を決める — 内省は「足す」操作であり、`superseded` を使わない** — Issue #104（#102 / #103 と同じ外部の採用検討者。「一般化された知識は人間が手で入れたものだけ、という状態が続く」）。🔴 **ADR 0087 / 0089 の論法はここでは使えない**——あちらは「除外一覧に載っていなかった」ことを根拠にしたが（ADR 0089 `:49-52` 自身が「`consolidate()` は無い。同じ背景系の相方である `reflect()` は名指しで外れているのに」と書いている）、**`reflect` は名指しで載っている。**⟹ 別の根拠を3本立てた: 除外の文言は**3箇所すべてで `実運用` を修飾**しており素の `reflect()` を外した文は**1本も無い**（`roadmap.md:26`/`:49`/`:148`、`AGENTS.md:47-48`）／§1.3 の締めが「**土台となる列・テーブルは Phase 1 に含める**」（`:53`）／より新しい **ADR 0082 `:216` が `reflect` の本体を実装する人を名指しで想定**している。⛔ **正典（north-star / roadmap / AGENTS.md）を1文字も書き換えていない。**🔑 **芯は「形の決定を実運用の決定から切り離す機構」で、それは `target` を必須にすることだった**——`target` の無い `reflect` は自身が「何を見るか」を決める＝**Background Cognition の実運用の決定**になる（`roadmap.md:148` が Phase 3 に置くもの）。⟹ ⛔ **Issue #104 提案の `reflect(ctx,{maxCandidates?,dryRun?})` を採らない。`target` が Phase 3 との接続点そのものである**（Scheduler 側に `ReflectTarget` を作る主体を置けば、この ADR を1つも覆さず実運用へ繋がる）。🔴 **`consolidate` と意味論が正反対**: N→1 の**置換**ではなく「足すだけ」で、**既存の行の `status` を1つも動かさない**——`memory-model.md:921`（§11 行5）がトリガーに名指しするのは「抽出・**統合**パイプライン」だけで内省は入っておらず、一般化された記憶はそれが一般化した観測を古くしない。⚠ **そして北極星の問い2**（オーナー §42-6 逐語「Background Cognition を無効にしても Memory Framework として成立する」）**がこの案を選ばせた**——既存の行を書き換える `reflect` は「無効にしても成立する」を満たさない。🔴 **LLM が「一般化するものは無い」と断れるスキーマにした**（`z.discriminatedUnion("outcome",…)`）——`consolidate` は「束ねようがない」を LLM を呼ぶ前に判定できるが reflect は中身を読まないと決まらず、**断れないスキーマはモデルに毎回捏造させる**（issue 自身が「事故になりやすい」と書いている）。⛔ **`strength` を下げない**（issue の提案を却下）——`provenance.ts:6-7` 逐語「原則7 は**追加のフラグではなく `kind` の値そのものとして実装される**」であり、下げるのは同じ区別の**2本目の信号**＝独立にずれる面（ADR 0082 の根）。加えて ADR 0078 が意図的に閉じた口であり、同 `:329-331` が「**下げ幅によっては `below_threshold` で落ちる**」と名指ししている ⟹「弱く始める」は「消える」でもある。🔴 **土台に `reflected` を採らない**（自己増幅を形で止める。北極星の問い1——増やす操作が自分の産物を土台にできると「増やした分だけ想起が良くなるか」に原理的に答えられない）。🔴 **冪等性は買わない。買えない**——産物は `sourceObservationId: null` なので部分一意索引（`0001_init.sql:106-108`、`WHERE source_observation_id IS NOT NULL`）の**外**、ADR 0089 の「読んで status で弾く」は決定4 により**使えず**、`MemoryStore` に `content_hash` の口は**無い**（足せば第三者の adapter を壊す）⟹ **塞がず、2回呼ぶと2件できることを歯で固定**し、**その歯自身に「赤くなったら直すのは歯ではなく ADR」と書かせた。**⚠ **`「互いに素」は片方向だけだった【実測】**——`z.object` が未知キーを黙って剥がすため reflected 候補は consolidation スキーマも通る。実害のある向き（consolidation 候補 → reflection スキーマ）は閉じており、残りは `DeterministicLLMProvider` の**試行順**が支えている ⟹ 安全は形と順序の2つに乗っており、形だけでは買えていない（`.strict()` は3スキーマまとめての判断なので採らず、歯に測って残した）。⚠ **変異試験: M7（判定の優先順）は最初の歯では噛まなかった**——`status !== 'active'` かつ `kind === 'reflected'` が同時に成り立つ入力が無く、**順序を変えても答えが変わらない入力しか測っていなかった** ⟹ その入力を持つ歯を足して初めて赤くなった（等価変異 E1 は緑のまま）。⚠ 負債: `reflect` は冪等でない／issue 提案2つ（自動選定・強度を下げる）が意図的に未達／`ReflectedProvenance.sources` の型は緩いまま（`.min(1)` へ締めるのは破壊的変更なので提起どまり）／`meta.reason` が3値目になった（ADR 0089 負債3 が1段重くなった）／🔴 **`memory-model.md` §11 に内省の書き込み行が無い——ただしこの欠落は `consolidate` の時点で既に在った** | 採用 (2026-09) |
| [0092](./0092-lexical-or-coverage.md) | **クエリ語彙を OR で結び、`lexicalMatch` を被覆率にする** — ADR 0084 §10 が「独立した変更として採るべき」と予告していたものを実装する。ASCII の語どうしを AND で結んでいた旧契約では、英語の自然文（`what did we say about PROJ-1234`）は全語を含む記憶しか返らなかった。`LexicalHit` に `coverage`（一致した語彙数 ÷ クエリ語彙数）を足し、**返り値の順序契約を「`rank` 降順」→「`coverage` 降順、同値なら `rank` 降順」に変える**。`ScoreBreakdown.lexicalMatch` は `coverage` をそのまま持ち、🔴 **旧定数 `LEXICAL_MATCH_VALUE`（公開 export）を削除する破壊的変更**を伴う。postgres 実装は各クエリ語を `"..."` で囲んでから OR で畳む（`migrations/0009_memories_lexical_or_coverage.sql`。囲むのは websearch 演算子の誤解釈を防ぎつつ識別子の隣接要求を保つため）。⛔ **閾値は足さない**——被覆率は IDF を持たないので根拠のある値が無く、ADR 0084 §8「低選択率のクエリ」の負債はこの PR でも塞がっていない。⛔ **日本語の分かち書きは混ぜない**（人名は今も引けない。別の決定）。⭐ **【実測・CI】草稿が「疑わしい」と名指しした2点（`array_agg(DISTINCT tsquery)` と文字列連結からの `::tsquery` 再パース）は、run `34437557302` で両方とも通った**——報告者の問いの形を1件ずつ通す歯8本が全部緑で、`idx_memories_lexical` も新しい述語で選ばれ続けた（`Bitmap Index Scan` cost 1165.85／索引を落とした対照は `Seq Scan` cost 6227.99）。⚠ **打ったのは CI であり書き手ではない。**⚠ **`mnemora_lexical_coverage` の実行コストは測っていない**（候補行ごとに `to_tsvector` を再計算し、`ORDER BY` のため `LIMIT` 前の全候補に掛かる） | 採用 (2026-09) |
| [0093](./0093-extension-verify-mode.md) | **`CREATE EXTENSION` を発行できないロールのための口 — 「作らない」ではなく「検査する」** — 実例: `virchamate/virchamate-backend` への導入で、DB オーナーが承認したのは `btree_gin`/`pgcrypto` の2文だけで、mnemora が発行する `vector` は承認範囲の外にある。現物を読んで確認: `CREATE EXTENSION` は独立した**2経路**から出る——`schema` 指定時だけの `migrate.ts` の `REQUIRED_EXTENSIONS` ループ（経路1）と、`schema` の有無に関わらず必ず走る `migrations/0001_init.sql` 本文（経路2、`migrate.ts` 自身が二重管理を「意図的」と明記）。🔴 **オーナーの線**: `skipExtensions: true` のような素のブール値は fail-open なので却下——`RunMigrationsOptions.extensionMode?: "create" \| "verify"`（既定 `"create"`、**1バイトも変えない**）を足し、`"verify"` は `CREATE EXTENSION` を一切発行せず `pg_extension` を読んで確認するだけにする。両経路とも塞ぐ——経路1はループごとスキップ、経路2は `client.query()` へ渡す直前に `CREATE EXTENSION IF NOT EXISTS <name>;` 単体行だけを取り除く（`stripCreateExtensionStatements`。**`migrations/*.sql` のファイル自体は1バイトも書き換えない**）。抽出規則は `schema-namespace.test.ts` の既存の突き合わせの歯と`matchCreateExtensionLines`を共有し、正規表現の書き写しを避けた。「検査していない/検査したが無かった/在った」の3状態は `extensionMode` 省略（`extensionCheck: undefined`）・`MissingExtensionsError`（`instanceof` で判別、足りない拡張名と実行すべき SQL を書く）・`result.extensionCheck.verified` の3つで区別する。⛔ **`0001_init.sql` の編集は採らない**——ADR 0057「採らなかった案」が既に同種の理由（出荷済みマイグレーションの書き換え）で却下済みであり、マネージャーの「既存利用者には無影響のはず」という前提は**検証していない**（採らなかったので検証不要だった、が正確な言い方）。⭐ **PostgreSQL 本体のソース（`extension.c` の `CreateExtension()`）を読んで確認**: `CREATE EXTENSION IF NOT EXISTS x` は `x` が既に存在すれば権限チェックへ到達する前に早期リターンする——⟹ **「拡張が既に全部揃っている」状況では既定の `"create"` モードも権限の無いロールで成功してしまう**。verify モードが明確に優位なのは virchamate の実例どおり**未設置**の場合（`create` は生の権限エラー、`verify` は制御された `MissingExtensionsError`）。CI（本物の PostgreSQL 17 + pgvector）で、`CREATE EXTENSION` 権限を持たない実ロール（`public` への `CREATE` を明示的に `REVOKE`）を使い、この2つの失敗を対照させて実測した | 採用 (2026-09) |
| [0094](./0094-identifier-probes-local-embedding.md) | **識別子・固有名詞の probe を、鍵もカセットも要らないローカル埋め込みで測る** — Issue #109 の項目1（ゴールデンセットを数十件へ）は [ADR 0088](./0088-retrieval-quality-measured-in-ci.md) が**明示的に閉じなかった**もので、同 ADR は「⟹ 順序は『実 API で録り直せる手段』または『**入力が固定されない埋め込み provider**』が先である」と書いていた ⟹ **この ADR は後者に従う**（ADR 0085 の `@mnemora/local-embedding` は鍵もカセットも要らないので probe を自由に足せる）。【現物】既存 probe 7件はすべて日本語の query で、**ASCII の識別子・固有名詞を含む query は0件**だった＝#106 が名指しした用途を1件も測っていなかった。⟹ 12件（人名/チャンネル名/社内システム名/案件コード/チケット番号）を足し、**3群を別々に**集計する。🔴 **数字には arm・`(provider, model, dimensions)`・haystack の3つを必ず添える**——⚠⚠ **256次元は両空間で同じなので次元数では区別できない。**【実測】新 `japanese`(7件, `(local, ruri-v3-30m/sym, 256)`, llm=deterministic, 疎) は **MRR 0.810 / hit@1 5-7 / hit@10 7-7**、新 `identifiersSparse`(12件, 同空間, 疎)・`identifiersDense`(12件, 同空間, **密**) はともに **MRR 1.000 / hit@1 12-12**。⭐ **arm B との比較は副産物ではなく、この製品が必要としている比較そのもの**（`packages/local-embedding/README.md` が「確かめていないこと」第1項で名指しした欠落）——arm B（`(openai, text-embedding-3-small, 256)` 記録再生, llm=deterministic, 疎, MRR 0.714 / hit@1 4-7 / **hit@10 6-7**）と probe も LLM 層も同一で**差は埋め込みだけ**。⚠ 標本7件 ⟹ ⛔ 優劣を一般に主張しない（ADR 0033 §3）。⚠ **`MRR 0.714 / hit@10 7/7` という arm は存在しない**（`hit@10 7-7` は arm C・MRR 0.738）。🔴🔴 **#106 の失敗は疎・密の両条件で1件も再現しなかった（hit@1 12/12）——⛔ これを勝ちに見せない。**言えるのは「この埋め込み・この probe 集合・この haystack では再現しなかった」までで、⚠ **`(openai, text-embedding-3-small, 256)` ではこの12 probe を測れない**（カセットに記録が無い）⟹ ⛔「OpenAI なら失敗する／しない」とも言えない。密 haystack は `sparse` の値を見た**後に1度だけ設計し1度だけ測った**（⛔ 欲しい数字が出るまで的を動かしていない）。⛔ **既存に1文字も触らない**（`probe-set.ts` / arm A・B・C / `retrieval-baseline.json` / `retrieval-quality` ジョブ）——**arm ごとに probe 母数が違う数字が1つのファイルに同居する形は「存在しない arm の合成」を*増やす*方向**だからである（3度起きており ADR 0068 が一本費やして塞ごうとした）。⛔ **門にしない**（標本7・12件では偽陽性。ADR 0088 §2.1）——⚠ 「なぜ*今は*か」: **probe を増やした後、その母数で偽陽性率に上限を置けると測れてから別途決める。**⭐ **そして §8 が正直な再発の記録である**——要約スクリプトが基準値ファイルを**読んでおらず**、その理由に「⛔ 門にしない」を挙げていた。🔴 **「⛔ 門にしない」と「⛔ 基準値と比べない」は別のことであり**、ADR 0088 §3 は**両方を同時にやっていた**（diff を Job Summary に出し、かつ相違では落とさない）⟹ 比べない形は同 §3 が「⛔ この ADR は同じ形を繰り返さない」と名指しした形の**再発**だった。`--baseline` を足し、一致なら1行で黙り、相違でも `exit 0`、🔴 **比較項目に `embeddingSpace`/`haystackKind`/`label` を含める**（数字だけ比べると条件が変わったのに数字が同じ場合を「一致」と出す）、🔴 **`weights_unavailable` のときは比較を1つも出さない**（オーナー代理の逐語「『HF から取れなかった』が『想起の質が下がった』に見えることです」）。⚠ **【実測】その過程で `japanese` 群の `label` が `haystack=` を落としており基準値と食い違っていた**（基準値の `provenance.note` の一致項目一覧に `label` が入っていなかった）⟹ bench 側を直し、**label が自身の条件を落としていないことを検査する歯**を置いた。負債: **CI が HF への外向き通信に依存**（⟹ preflight `warmup()` と分岐を置いたが、**キャッシュが CI 上で実際に効くかは実測していない**）／標本は 7・12件のまま／新 arm は記録の再生では測れない／再発を止めたのは仕組みではなく人の目である | 採用 (2026-09) |
| [0095](./0095-embedding-provider-conformance.md) | **`EmbeddingProvider` の適合テストを新設する — 順序の検査は決定性を前提にしているので、決定性を「宣言させる」** — [Issue #116](https://github.com/takecchi/mnemora/issues/116)（ADR 0072 の負債1 と ADR 0085 の負債4 が指していた同じ穴）。`packages/testkit` の適合テストは **Store 系5種だけ**で provider 向けが1本も無く、一方 `EmbeddingProvider` の実装は**実質4つ**ある（`@mnemora/openai` / `@mnemora/local-embedding` / `DeterministicEmbeddingProvider` / `RecordedEmbeddingProvider`）。⚠ **ADR 0072 の却下理由「LLM は非決定的なので同じ出力を要求できない」は埋め込みにはそのままでは当たらない**（ADR 0090 の live テストがビット単位の一致を前提に境界を測れている）。⚠ ただし **「決定的な実装が在る」は「契約として決定性を要求してよい」と同じではない。**🔴 **芯は「順序の検査が決定性を前提にしている」こと**——「返る順序が入力順に対応する」は契約の要（ずれると memory とベクトルが1つずれて結び付く）だが、**実装非依存に測る方法は「並べ替えて結果も並べ替わるか」しかなく、毎回違うベクトルを返してよい実装には意味を持たない**⟹ **順序と決定性は切り離せない。**⟹ 決定: `describeEmbeddingProviderConformance(options)` を新設し、**無条件7本**（`space` の3欄が妥当／`space` が `embed()` の前後で不変／`embed([])` は `[]`／件数一致／各ベクトル長 = `space.dimensions`／成分が有限の数／1件は1件）＋ **`deterministic: true` のときだけ2本**（同一入力の再現性・順序の対応）。🔴 **`deterministic` は省略できない宣言にする**（`?` を付けない）——任意にすると **「決定的でない」と「宣言し忘れた」が同じ `undefined` に潰れる**（ADR 0090 決定2 と同じ形）。⭐ **`false` のとき歯は消さず `it.skip` で名前を残す**——テスト名が消えると「そもそも歯が無い」と「走って通った」がログから区別できない（`local-embedding` の live テストが `describe.skip` / `if (!live) return` を採らなかったのと同じ理由）。**`texts` は呼び出し側に注入させる**——`RecordedEmbeddingProvider` は記録に無い入力で例外を投げる（ADR 0051）ので **suite が文字列を決め打ちできない**。⛔ **バッチ不変性は検査しない**——本物のモデルは `padding: true` でバッチを処理する（ADR 0090 §1.4）ため**成立するとは限らず、測っていない。⛔ 測っていないものを契約にしない。**⛔ **決定性を無条件に要求する案は却下**（実 API の再現性の保証を持っていない ⟹ 適合テストが「実装が守れない契約」を主張することになる）。⛔ **「ゼロベクトルを返さない」を歯にする案も却下**——ADR 0040 は **recall 側の契約**であって provider への禁止ではない（**他所の ADR の結論を別の層の契約として言い換えない**）。代わりに置いたのは「成分が有限の数である」。⛔ **入力長の上限も歯にしない**（上限は実装ごとに違う。ADR 0090）。⚠ **負債: 当てたのは4実装のうち testkit 自身の2つだけ**（`@mnemora/openai` / `@mnemora/local-embedding` は走行中の PR と線のため別作業 ⟹ **「4つが同じ契約を満たす」ことはまだ確かめられておらず、緑になるかも分からない**）／バッチ不変性は未測定／**決定性の宣言は自己申告**で見破れない／**`LLMProvider` の適合テストは今も無い**（ADR 0072 負債1 の埋め込み側だけを返済した） | 採用 (2026-09) |
| [0096](./0096-bootstrap-local-embedding-onto-npm.md) | **`@mnemora/local-embedding` を npm へ載せる — 初版は OIDC で出せないので、tag 由来の中身を手元から1回だけ出す** — ADR 0085 で新設され `PUBLISH_TARGETS` にも入っているのに **registry に存在しない**（`npm view` が E404）。理由は2つで、どちらも「忘れていた」ではない: ① 最後の Release `v0.1.3` が指す `fe83e1f`（#91）の時点で **`packages/local-embedding` は存在しない**（【実測】`git ls-tree -d v0.1.3 packages/` に無い）／② **初版を OIDC で出せない**（【実測】[npm/cli#8544](https://github.com/npm/cli/issues/8544) は `state: open` / `updated_at: 2026-08-05`）。⭐ **【実測】預かった publish 用トークンは3段のうち1段しか打てない**——段0 publish は ✅、段1 `npm trust list` は 🔴 **E403**、段2（直接 publish の許可）は 🔴 **`npm trust github --help` にフラグが無く web UI 専用** ⟹ **トークンを預かっても3段は揃わず、段1・段2 はオーナーの 2FA が要る。**⭐ **段0 の能力は非破壊で測れた**——ADR 0067 が「`--dry-run` は副作用の直前の判定を構造的に検出できない」と実測済みなので予行では権限を測れず、代わりに**既に在る版へ本番 publish を試みた**（上書きは構造的に不可能なので**返る 403 の種類だけが情報になる**）: `You cannot publish over the previously published versions: 0.1.1` ＝ 権限の 403 ではなく**版の衝突** ⟹ 権限は在る（ADR 0066 測ったこと8 と同じ証拠の形）。`dist-tags` は `latest: 0.1.3` のままで `bootstrap-probe` も作られておらず**副作用ゼロ**を確認。🔴 **オーナーは当初 `0.1.3` に揃える案を提案していたが、`v0.1.3` に当該ディレクトリが無い**⟹ **どの commit も指さない `latest`** ができ、ADR 0066 が引き受け **ADR 0070 が `0.1.1` の公開をもって解消を宣言した負債の再導入**になる。**そして買おうとしていた「版の揃い」はこの案を選ぶ理由にならない**（版は tag が決めるので bootstrap が何であれ `v0.1.4` で6つとも揃う）⚠ **registry 上の揃いを見ている歯は無い**（`--expect-version` と検査項目2 が見るのは作業ツリーの `package.json`）。🔴 **【訂正】この作業者が同案に立てた反論「`^0.1.3` を要求するので古い core が入って壊れる」は間違いだった**——【実測】core から取るのは **`import type` の3つだけ**（`Ctx`/`EmbeddingProvider`/`EmbeddingSpaceId`）で、`EmbeddingProvider` interface は `core@0.1.3` の `.d.ts` と main の `.ts` が**一字一句同じ**（ADR 0090 も 0095 もこの2員を変えていない）⟹ **同案は技術的には成立した。落としたのは追跡可能性の1点のみ。**⚠ 測る前に反論として出したのが誤りで、AGENTS.md「確かめていないことは確かめていないと書く」に反していた。⟹ 決定（**オーナー逐語**「v0.1.4 を今切る → bootstrap は tag 由来の 0.1.4」）: **① 初版は `0.1.4` で中身は `v0.1.4` の tag 由来**（全版が commit を指す状態を保つ唯一の案）／**② Release を先に切り、publish run が1回赤くなることを受け入れる**——5つは OIDC + provenance で成功し末尾の `local-embedding` だけ 403。⭐ **この形は `publish-targets.mjs` が明示的に想定している**（逐語「未公開のものが途中に居ると、その後ろが publish されない」）⟹ **末尾に置く規律がこの赤を「他を巻き込まない赤」にしている。**段1・段2 後に再実行すれば冪等分岐で緑／**③ 手元の梱包も `apply-release-version.mjs` を通す**（`v0.1.4` を checkout しても `version` は `0.1.1`。手で書くと版の決め方が2箇所になり ADR 0070 の意味が消える）。⭐ **公開前に README を直した**——「**このリポジトリでは** postinstall を拒否している」しか書いておらず **`allowBuilds` は公開物に付いていかない**ので利用者に何も届いていなかった。【実測】`onnxruntime-node@1.24.3` の `install-metadata.js` の `requirements` は **`linux/x64` だけが `cuda12`**（302MB）で他5プラットフォームは `[]`、かつ `install.js` の既定経路は依存として入るとき早期 return しない ⟹ **効くのは大半の CI runner・Docker image・サーバで、手元の mac では起きないので気づくのは本番 image を焼くとき。**⛔ 却下: `0.1.4-bootstrap.0` + `--tag bootstrap`（**main を今リリースしたくない場合はこれが最善**——「覆るとしたら」側に残す）／初版も CI からトークンで（ADR 0066 が既に却下。加えて npm 自身が「2FA を迂回するトークンの**直接 publish** を制限中」と警告）／`PUBLISH_TARGETS` から一時的に外して緑で通す（**赤は情報である**。規律は赤を消すためではなく赤が他を巻き込まないために在る）。⚠ 負債: **`local-embedding@0.1.4` に provenance が付かない**（6中1つ・1版だけ。ADR 0066 の `0.1.0` と同種だが**中身は tag と一致する**ぶん範囲が狭い）／**赤い run が「正常だった回」として履歴に残る**（この ADR が唯一の説明）／🔴 **publish 用トークンがチャットに露出した**（会話ログに残る。**要 revoke**。門では検出できない手順側の負債）／**段0 の経路そのものが失われうる**／`PUBLISH_TARGETS` への足し忘れは今回も機械的に検知できない（**今回はたまたま足されていた**）／README に書いた `ONNXRUNTIME_NODE_INSTALL=skip` の npm 経路は未検証。⭐ **【実測・実行済み】`v0.1.4` を出した。6パッケージとも registry に `0.1.4` が在る**（5つは CI が OIDC + provenance、`local-embedding@0.1.4` は段0 として手元から）。🔴 **落ち方の予測は外れた——403 ではなく E404 だった**（run `34452890407`: 5つが `✔ publish した` のあと `npm error 404 Not Found - PUT .../@mnemora%2flocal-embedding`）。**順序の予測は当たり、落ち方だけが外れた**——末尾に置く規律は設計どおり働いた。**なぜ誤ったか: 参照した ADR 0070 に答えが書いてあったのに読み違えた**（同 ADR 測ったこと3 逐語「不一致なら npm は 404 を返す——registry は 403 を意図的に 404 で隠す。今回は `PUT` まで到達して『this action』を名指しした 403 ⟹ npm は身元を認識していた」）⟹ **403 は「身元は認識したが操作を許していない」、404 は「結び付ける先が無い」。**`v0.1.1` が 403 だったのはパッケージが既に在ったからで、初版は**不存在**なので 404 が正しい。⭐ **ADR 0066 の「読み取りが書き込みに遅れる」を再確認**——`+` を出した 08:05:57Z の後**約4分間、`npm view`（未認証・認証つき）も CDN を迂回する `?write=true` も 404** を返した ⟹ **この4分間に「失敗した」と判断して再実行すると E403 を見て混乱する。**⭐ **素の consumer で往復した**: `npm install @mnemora/local-embedding@0.1.4` が `^0.1.4` を **`@mnemora/core@0.1.4` に解決**し、`import` で 13 export が読め、`new LocalEmbeddingProvider().space` が**モデルを落とさずに** `{local, ruri-v3-30m/sym, 256}` を返した。⚠ **【実測】公開物の依存に high が5件**（`adm-zip` ← `onnxruntime-node` ← `@huggingface/transformers`、および `sharp` の libvips/libheif 継承。すべて `fixAvailable: false`）⭐ **脆弱な経路はどちらもこのパッケージが使わない経路**（`sharp` は画像入力、`adm-zip` は **postinstall がアーカイブを展開するところ**）🔴 ⟹ **README に書いた「postinstall を止めろ」は 302MB の節約だと思って書いたが、`adm-zip` の展開経路を踏まないことでもあった**（含意は執筆時に見えていなかった）。⚠ 追加の負債: **`0.1.4` の README は audit の5件を書いていない——公開された版の README はもう直せない**（`0.1.5` で足す。**素の consumer での往復を publish の前に一度もやらなかったのが順序の誤り**）／🔴 **段1・段2 が未了なので次の Release でも `local-embedding` は publish されない**（`v0.1.5` で再び赤くなる。今度は 403 のはずだが**これも予測であって実測ではない**——一度外している）。⚠ **確かめていない: 段1・段2 の後の緑の再実行**（6つ全部が飛ばされる形は走っていない）／**`0.1.4` が実際にモデルを落として推論できること**（`embed()` は一度も呼んでいない） | 採用 (2026-09) |
