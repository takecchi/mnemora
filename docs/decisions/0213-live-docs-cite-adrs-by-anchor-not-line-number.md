# ADR 0213: 行番号での引用は、この repo 自身の「ADR は書き換えず追記する」作法によって腐る — 生きた文書はアンカーで指し、歯で止める

- **状態**: 採用 (2026-09)
- **日付**: 2026-09-17

**⚠ 各主張の出所を分ける**（[ADR 0207](./0207-dry-run-reads-existence-and-coverage-degrades-silently.md) /
[ADR 0209](./0209-dry-run-short-circuit-predates-adr-0207-and-is-counted-by-machine.md) と同じ体裁）。

- **【現物】** — この repo のコード・文書を書き手が読んで確かめた。
- **【実測】** — この書き手が自分の手で走らせて確かめた。
- **【受】** — 報告として受け取り、再導出していない（出所を明記する）。

---

## 問い

[ADR 0209](./0209-dry-run-short-circuit-predates-adr-0207-and-is-counted-by-machine.md) の
**測ったこと7** は、PR #454 が ADR 0067 の冒頭に10行入れたことで、0067 を行番号で指している
引用がずれたことを記録し、**「直すのは別の PR に譲る」**と書いて止めた。

⚠ **そのとき 0209 が書いた件数は「4箇所」である。**⟹ **数え直したら、違った。**

---

## 測ったこと【実測】

**すべて 2026-09-17、`origin/main` = `cdb59a7` の木で測った。**

### 1. 🔴 数え上げは、3回とも「書き方の一族」ごと落ちた

| 回 | 出した数 | 落としていた書き方 |
|---|---|---|
| 1（ADR 0209） | **4箇所** | ADR 0067 以外の引用先を見ていなかった |
| 2 | **15件** | `` [ADR 0078](./0078-….md)`:251-253` `` / `` 同 `:326-331` ``（**継続引用**） |
| 3 | **`release-v1.md` に6件** | `` `docs/decisions/0070-....md:139-142` ``（**ファイル名をリテラルな `....` へ省く形**）。この形だけで同ファイルに10件 |
| 4 | **「生きた文書のアンカーは全部実在する」** | **`ADR NNNN「…」` の形しか見ておらず、`` [ADR NNNN](link)「…」 `` を数えていなかった。**実際は3ファイルに**61件**在り、**14件が実在しない**（下の 8） |

⟹ ⭐ **単一の正規表現は、静かに過少検出する**（`docs/autonomy.md` §4.1 の「静かに失敗する道具」の族）。
**「エラーが出なかった」ではなく「取りこぼしていないこと」を測らないと、数え上げは信用できない。**

### 2. 生きた文書から ADR を行番号で指している箇所は **18件**、3ファイル

**【実測】**`docs/release-v1.md` **16件**、`docs/recall.md` **1件**、`docs/roadmap.md` **1件**。
⚠ **後ろの2件は、一度「ADR への引用ではない」と誤判定した**——同じ行に
`recall-runtime.ts:805` という別の引用が同居していたのを読み違えた。
**【現物】**現物は `` [ADR 0172](./decisions/0172-….md) の `:249` `` であり、**ADR への行番号引用である。**

### 3. repo 全体では **762件**

**【受】**作業者が全ファイルを走査して分類した（**この書き手は総数を再導出していない**。
下の 4 だけを自分で全数検算した）:

| 引用先 | 件数 |
|---|---|
| **A: ADR（`docs/decisions/*.md`）** | 41 |
| **B: ADR 以外の文書** | 121 |
| **C: ソースコード** | 600 |
| 外部リポジトリ / npm 依存 | 16 |

⚠ **B・C から各10件を抜き取った標本では、B が 5/10、C が 6/10 ずれていた。**
⛔ **これは20件の標本であり、母集団の率ではない。**

### 4. 🔴 `docs/release-v1.md` の全数検算: **63件中 7件（11%）がずれ**

**【受】**作業者が全数を1件ずつ開いた。**【実測】**そのうち**7件すべてを書き手が自分で開き直した。**

| 引用元 | 指している先 | 本当の場所 |
|---|---|---|
| `:46` | `ci-green-check.mjs:34-35` | **40-41行**（§0 の**通過条件**） |
| `:121` | `generate-adr-index.mjs:14-21,45-62` | 前半が無関係（§0.3 の通過条件） |
| **`:140`** | `adr-index-freshness-branch-lib.mjs:1-18` | 🔴 **ポインタではなく主張が誤り**（下の 5） |
| `:650` | ADR 0067、124-130行 | **137-138行** |
| `:695` | ADR 0067:118-134 | **128-144行** |
| `:1136` | `docs/autonomy.md:232` | **317行** |
| `:1402` | ADR 0067:100-103 | **112-113行** |

🔴 **⟹ 書き手はこの全数検算の前に、「標本の率（B 50% / C 60%）が当日の手順書にも当たるなら
半分近くが間違っている」とマネージャーへ報告していた。それは過大だった。**
⭐ **測っていないファイルへ、別のファイルで取った率を当てたことが誤りである**
——**ADR 0209 が「独立でない根拠を重ねるな」と結論したのと、同じ形の誤りである。**

### 5. `:140` だけは、ポインタではなく主張が誤っていた

**【現物】**`docs/release-v1.md` §0.3（**tag を切る前の通過条件**）は
「鮮度の歯は **`main` に限って**赤くなる設計であり」と書いている。
**だが引用先のファイルが、その引用範囲の中で自分から否定している**（**【現物】**逐語）:

> ADR 0192 で「CI の `pull_request` でも有効にする」を足した結果、両者はもう同じ問いではない。

⟹ **結論（「0.1 が緑なら、その sha の索引は最新である」）は変わらない**——**網羅範囲はむしろ広い。**
⛔ **だが理由が違う。**

### 6. 🔴 ADR 0209 の測ったこと7 の表は、1件が誤り

**【実測】**`docs/release-v1.md` → `ADR 0067、82-114行` を 0209 は「ずれている」と書いたが、
**現物を開くと合っている。**範囲は +10 ずれたが、(B) 節の実体（run 比較表と E403 のエラー文と
因果の限界の注記）が**今もその窓に収まっている。**
⟹ **壊れていたのは4件ではなく3件**（`650` / `695` / `1402`。最後の1件は 0209 を書いた時点では
数えられていなかった）。

### 7. ⭐ 腐りの引き金は、この repo 自身の作法である

**【受】**作業者が ADR 201本の1行目と直後のブロックの blame を突き合わせ、
**「冒頭に訂正・前方参照ブロックを足した」コミットを8本・9ファイル**特定した
（2026-09-07 〜 09-17。**うち 09-17 だけで3コミット×4ファイル**）。

🔴 **この作法は ADR 0064 が正式に採った規律である。**⟹ **今後も同じ頻度で起き続ける。**

⭐ **そして、この PR 自身がもう一度それを踏んだ**——**【実測】**上の 5 を訂正するために
`docs/release-v1.md` §0.3 へ18行を追記したところ、**同じファイルの以降の行番号がすべて +18 ずれた。**

### 8. ⭐ アンカーの実在検査は、**歯にできない**

**【受】**作業者が `ADR NNNN「…」` を repo 全体で数えたところ **605件**あり、
**205件が引用先の本文に逐語では存在しなかった。**
**【実測】**書き手が現物で数え直した実例: あだ名 `「無いには種類がある」` は
**repo 全体で18箇所**（この PR が足したテストの2件を除く）で使われている。
⛔ **だが ADR 0008 の見出しは「「無い」を分類して返す」であり、
「無いには種類がある」は 0008 の本文に0回しか現れない**（`grep -cF` が **0**）。
⟹ **定着したあだ名ほど、逐語では存在しない。**

🔴 **そして「生きた文書3本に絞れば緑になる」も誤りだった**——**【受】**作業者が3本に絞って
測ると **61件中14件が「実在しない」。**⟹ **【現物】**書き手が中身を見たところ、
`ADR NNNN「…」` は**3つの違うものに使われている**:

| 使われ方 | 例 |
|---|---|
| **1. 本物のアンカー** | `ADR 0070「測ったこと1」`（引用先に実在する） |
| **2. あだ名** | `ADR 0008「無いには種類がある」`（本文に無い） |
| **3. 逐語の引用そのもの** | `docs/roadmap.md` の `[ADR 0114](…) **検討して採らなかった案5**: 「…」`——**`「」` はアンカーではなく引用文** |

⟹ ⛔ **構文だけでは3つを区別できない。**⟹ **実在検査を赤判定の歯にすることはできない。**

---

## 決定

### 1. 生きた文書は、ADR を行番号で指さない

**「生きた文書」＝ いま従う指示として読まれるもの**（`docs/**/*.md` のうち `docs/decisions/` を
除いたもの・`AGENTS.md`・`README.md`）。⟹ **引用先に実在する文字列（見出し・太字ラベル・逐語）で指す。**

```
ADR 0067「⭐ (B) の一般化 — この ADR の芯」
```

### 2. ⭐ アンカーが腐らないのは、行番号を壊しているのと**同じ規律**のおかげである

**ADR の本文は書き換えない**（ADR 0064）。⟹ **一度実在した文字列は、そこに在り続ける。**
⟹ **冒頭への追記はアンカーを1文字も動かさない。**

🔑 **同じ1つの規律が、行番号を腐らせ、アンカーを守っている。**
⟹ **だからこの移行は「問題を別の形へ移した」のではない。**

### 3. 歯は**1本だけ**置く。**生きた文書に限る**

`scripts/adr-citation-lib.mjs`（純関数）＋ `scripts/__tests__/adr-citation.test.mjs`。
ルートの `pnpm run test` に相乗りする（`identifier-probes-readme-freshness` と同じ形。
CI への配線は要らない）。

**歯: 生きた文書に、ADR への行番号引用が1件も無いこと。**

⛔ **アンカーの実在検査は、歯にしない**——測ったこと8。**構文で3つを区別できない。**
⟹ 代わりに、**この PR が入れた15種のアンカーだけ**を名指しで検算する歯を置く
（**歯ではなく移行の検算である**、とテストに明記した）。
⭐ **その一覧が腐らない理由は決定2 と同じ**——**ADR の本文は書き換えない。**

### 4. ⛔ 件数を歯に焼き込まない

**再現率は、現物ではなく静的な fixture で測る**——18件の**古い書き方**をテストの中に
リテラルで持ち、検出器が1件残らず拾うことを見る。
⛔ **「repo 全体で N 件」を assert する歯は置かない**——**ADR が1本増えるたびに落ちる。**
それは歯ではなく snapshot である。

### 5. ポインタは直す。主張は追記で訂正する

| 種類 | 扱い | 理由 |
|---|---|---|
| **ポインタ**（宛先の書き方） | **その場で直す** | **住所であって、「当時こう判断した」という記録ではない。**古い住所を残す意味が無い |
| **主張**（測ったこと7 の `:140` のような） | **消さず、追記で訂正する** | **記録である。**この文書の作法（ADR 0064） |

### 6. ADR 本体（`docs/decisions/*.md`）の行番号引用は直さない

**【受】**A の41件のうち **20件は ADR が ADR を行番号で指している。**
⛔ **直さない**——**ADR は当時の記録であり、本文を書き換えない**（ADR 0064）。
⟹ **歯の判定対象からも外す。**⚠ **検出器そのものは ADR も走査する**
——**外すのは判定の段であって、検出の段ではない。**方針を変えるときに検出器を書き直さずに済む。

### 7. ADR 0209 の本文は書き換えない。前方参照だけを足す

測ったこと6 の訂正はこの ADR に置き、0209 へ足すのは冒頭の1ブロックだけである
（ADR 0209 が 0207 に対して採ったのと同じ形。**【実測】**その形が10行の追加・0行の削除であることを確かめてある）。

---

## 採らなかった案

- **762件を全部直す。**⛔ 採らない——**C（ソース600件）は ADR が「当時こうだった」と
  記録しているものであり、いまの行へ追いかけて直すのは記録の書き換えである。**
- **アンカーの実在検査を歯にする**（repo 全体でも、生きた文書3本に絞っても）。
  ⛔ 採らない——測ったこと8。**repo 全体で 605件中205件、3本に絞っても 61件中14件が
  「実在しない」と出る。**⟹ **それらは壊れているのではなく、あだ名か逐語引用である。**
  ⚠ **あだ名を禁じる案も採らない**——**`「無いには種類がある」` だけで18箇所定着しており、
  読みやすさに寄与している。**これは別の主張であり、別の PR の領分である。
  🔴 **この案は、書き手が最初に設計したものである。**実測が潰した。
- **既知の違反を snapshot に記録し、増えたときだけ赤にする**（`check-public-api-surface.mjs --write` の形）。
  ⛔ 採らない——**`docs/autonomy.md` §4.1 が、まさにその道具で
  「古い `dist/` のまま `--write` すると、他人が入れた変更を snapshot から消してしまう」
  という静かな失敗を記録している。**⟹ **同じ穴を新しく掘らない。**
- **行番号を「いま正しい番号」へ書き直す。**⛔ 採らない——**また腐る。**
  測ったこと7 のとおり、この PR 自身が18行の追記でずらしている。
- **規約だけ書いて、機械化しない。**⛔ 採らない——**既に2回踏んでいる**
  （ADR 0067 の +10 と、Issue #377 の `recall.ts:1239` → 現物は `:1370`。後者は
  **【実測】**書き手が確かめた）。**注意力では止まらない。**
- **ADR 本体の行番号引用も直す。**⛔ 採らない——決定6。**ADR 0064 と正面からぶつかる。**

---

## 引き受けた負債

- **🔴 B（121件）と C（600件）に手を付けていない。**⚠ **抜き取り20件では半数前後がずれていた**
  ——⛔ **だがこれは標本であり、母集団の率ではない**（測ったこと4 が示すとおり、
  全数で測ると率は大きく変わりうる）。**どのファイルが実際にどれだけ腐っているかは、
  `docs/release-v1.md` 以外では測っていない。**
- **🔴 `docs/roadmap.md` に179件の引用が在る**（**【受】**。repo で最多）。**1件も検算していない。**
- **⚠ ADR → ADR の20件は、ずれたまま残る。**決定6 の通り意図的である。
  ⟹ **ADR を読む人は、そこに書かれた行番号を信じてはいけない。**この ADR はそれを直さない。
- **⚠ 歯2 はアンカーの「実在」しか見ない。「一意」は見ない。**
  同じ文字列が引用先に複数あっても通る。**【実測】**移行した18件はすべて `grep -cF` で
  **1** を確かめたが、**それを機械が強制してはいない。**
- **⚠ 全角ダッシュを使う形**（`1145〜1200行付近`）**は、意図的に検出していない。**
  **【受】**作業者の実測: 拾いたい実例（間隔24字）より**拾ってはいけない実例**
  （`ADR 0067 は `#` の見出しを1行目にしか持たず`、間隔12字）のほうが**間隔が短く**、
  文字数窓では区別できない。⟹ この形は ADR 本体にしか無いので、いまは実害が無い。
  **生きた文書にこの形が現れたら、歯は見逃す。**

---

## これが覆るとしたら

- **`docs/decisions/` の本文を書き換える作法へ変えたとき。**⟹ **決定2 の根拠が消え、
  アンカーも腐るようになる。**そのときは「引用先の逐語も一緒に持つ」形が要る。
- **「あだ名」を禁じる規約が入ったとき。**⟹ **歯2 を repo 全体へ広げられる。**
- **`docs/roadmap.md` を当日の手順として読む運用になったとき。**⟹ **179件が
  「生きた文書」の側へ移り、この ADR の射程に入る。**
- **検出できない書き方が新しく現れたとき。**⟹ **歯は静かに見逃す。**
  ⭐ **だから再現率の fixture は、新しい書き方を見つけるたびに足すこと。**

---

## 確かめたこと / 確かめていないこと

- **確かめた（この器で実行）**: 測ったこと1・2・4（7件すべてを自分で開き直した）・5・6・7の後半
  （自分の追記が18行ずらしたこと）・8の実例（ADR 0008 のあだ名）。
  移行した18件のアンカーが `grep -cF` で **1** を返すこと。
  Issue #377 の `recall.ts:1239` が現物では `:1370` であること。
- 🔴 **確かめていない**:
  - **repo 全体の762件という総数**（**【受】**。分類も再導出していない）。
  - **B・C の抜き取り20件**（**【受】**。書き手は開いていない）。
  - **ADR 冒頭への追記コミットが8本で全部であること**（**【受】**。
    「訂正」「追記」という語を含まない同種の編集は検出網に掛からない）。
  - **605件中205件という内訳**（**【受】**。書き手が現物で数え直したのは ADR 0008 のあだ名1種（18箇所）だけである）。
  - **`docs/roadmap.md` の179件**——**1件も見ていない。**
  - **歯が CI で実際に走ること**——`vitest.config.mts` の `include` に一致する配置にしたが、
    **CI 上での実行は確かめていない**（この PR の CI が最初の実測になる）。
