# ADR 0059: 段1の ANN クエリで `period` を絞る（`COALESCE(occurred_at, recorded_at)` の式索引を1本足す）

- **状態**: 採用 (2026-09)

- **文脈**:

  `docs/recall.md` §3 の段1（ANN 検索、`VectorFilter` を渡す `VectorStore.search`）は、
  `tenant_id` / `status` は絞るが、`period`（`occurredAfter`/`occurredBefore`）は絞らずに
  ANN を実行し、`period` は段2（`recall-runtime.ts` の後段フィルタ）でのみ捨てていた。
  これは[ADR 0023](./0023-subject-filter-in-ann-stage.md)が明示的に決めたことである——
  `subjectId` は等値比較なので段1へ降ろしたが、`period` は連続値の範囲比較であり、
  `docs/recall.md` が指摘する「partial index は離散値・低カーディナリティのフィルタに
  向くが、連続値の範囲比較には向かない」（`docs/recall.md:135`）という制約に関わる
  ——降ろすには索引設計（バケット列か式索引か）に踏み込む判断が要るとして、
  ADR 0023 の範囲では見送った。

  ⟹ ADR 0023 が `subjectId` について指摘したのと同じ構造の穴が `period` にも空いている。
  **大規模テナントで狭い時間窓（例:「直近1日」）を指定すると、over-fetch の窓
  （`k' = limit × overFetchFactor`、既定40）がテナント全体の近傍で埋まり、
  窓内の記憶が1件も残らないまま黙って落ちうる。**[ADR 0056](./0056-exclude-provenance-kinds-in-ann-stage.md)
  は同じ構造の穴を `excludeProvenanceKinds`（離散値の等値比較）について塞いだが、
  `period` は「今回も降ろさない」と明記して見送った——ADR 0023 の却下理由（連続値の
  範囲比較）がそのまま残っていたためである。

  **本 ADR で、その却下理由を実際に解消する**: 式索引
  `(tenant_id, status, COALESCE(occurred_at, recorded_at))` を1本追加することで、
  連続値の範囲比較を索引で支える。ADR 0023 が「バケット列を足すか、というスキーマに
  踏み込む判断が要る」と書いた、その判断そのものである。

  `docs/roadmap.md:249`（オーナー回答 2026-09-06、マネージャー経由で伝達）は
  「テナントあたりの想定記憶数は100万件級を前提に設計する」と確定している。
  ⟹ 本 ADR が扱う穴（狭い窓 × 大規模テナントの取りこぼし）は、この前提の下で
  実際に起こりうる規模の問題として扱う。

- **決定**:

  1. **段1の `WHERE` 句に `period` の述語を降ろす。** 比較対象は
     `COALESCE(occurred_at, recorded_at)`（[ADR 0039](./0039-period-boundary-conformance.md)が
     定義した「実効時刻」——4箇所あった判定規則の5箇所目になる）。両端とも包含
     （`occurredAfter` は `>=`、`occurredBefore` は `<=`）——`RecallQuery.occurredAfter`・
     `memory-store.ts` の `aggregateScope` が既に使っている厳密経路と同じ境界の含み方に
     揃える。
  2. **`VectorFilter`（`packages/core/src/interfaces/vector-store.ts`）に
     `occurredAfter?: Date` / `occurredBefore?: Date` を足す。** `recall-runtime.ts` の
     段1呼び出しに `occurredAfter: scope.occurredAfter` / `occurredBefore:
     scope.occurredBefore` を渡す。
  3. **段2の後段フィルタ（`effectiveTime` による `continue`）は残す。** ADR 0023・ADR 0056
     と同じ多層防御——`VectorFilter` は adapter が実際に適用しなければならない契約
     （ADR 0034）だが、正しさの責任は後段にも置く。
  4. **`(tenant_id, status, COALESCE(occurred_at, recorded_at))` の3列の式索引を1本、
     追加だけで入れる**（`packages/postgres/migrations/0003_period_ann_stage_index.sql`、
     `idx_memories_period_ann_stage`）。**既存の `idx_memories_recall_gate`
     （`tenant_id, status, decay_floor_at`）は作り直さない。** 詳細は「採らなかった案」参照。
  5. **`hnsw.iterative_scan` は入れない。** これは「良い案だが今回は採らない」ではなく、
     **別の PR・別の ADR の腕として意図的に切り出した**——`docs/recall.md` §3 が
     「フィルタ問題」と「スコア問題」を別機構で解くと決めた枠組みの中で、`period` の
     押し下げは前者に対する一つの手（索引側で候補を絞る）であり、`hnsw.iterative_scan`
     はもう一つの独立した手（ANN の探索側を深くする）である。両方を同じ PR に混ぜると
     「どちらの効果か」が実測で切り分けられなくなるため、腕を分けた。棄却したのではなく、
     **測る対象を1PRにつき1つに絞った。**

- **理由**:

  1. **ADR 0023 が `period` を降ろさなかった理由は、索引設計に踏み込む判断が要るという
     一点だった。** 本 ADR はその判断を実際に行っている——式索引という形で、連続値の
     範囲比較を索引に支えさせた。ADR 0023 の却下理由は解消された。
  2. **段1と段2の二重構造そのもの（多層防御）は変えない。** `subjectId` /
     `excludeProvenanceKinds` と同じ枠組みに `period` を合流させるだけであり、
     `VectorStore` の契約（絞る義務を持つが、正しさの責任は後段にもある）に
     新しい例外を作らない。
  3. **式索引を選び、バケット列を選ばなかった。** バケット列（例:「直近30日」)は
     離散化のために新しい列とその再計算ロジックが要り、粒度の設計（何日単位か）という
     別の判断を持ち込む。式索引は既存の2列（`occurred_at`/`recorded_at`）から
     `COALESCE` で計算できる値をそのまま索引化するだけで、新しい列もバッチ更新も
     要らない。

- **⚠ 実測値 — 出どころの明記**:

  以下の数値は、**別の委譲（前任者）が 100,000行・256次元・1点のみで測ったと報告した値
  である。前任者自身が「引き写しであり裏取りしていない」と書いている。**
  以下は伝聞の引き写しとして扱うこと——**ただし本 PR では、この主張の中心部分だけを
  別途この器の Postgres で再現した。下の「⭐ この PR で一次実測した分」を参照。
  再現できた部分と、再現しなかった部分がある。**

  - 狭い窓（0.1%）では、96件が実在するのに `recall()` は0件を返す。この索引（本 ADR、
    以下 P2 と呼ぶ）を入れると40件返る——厳密・基準線（索引なし）より速い
    （1.9ms vs 8.5ms、と報告されている）。
  - P2は現状より悪くなる窓が無いと報告されている——広い窓（10%/50%）では現状＋後段
    フィルタと構造的に同一プランになり、返却数も一致する（10%: 7 vs 7、50%: 21 vs 21）。
    ⟹ 実測4点（0.1%・1%・10%・50%の4窓、と推定される。前任者の報告に窓の一覧は
    明示されていないため、本 ADR で断定はしない）で退行なしの片側改善、という報告。
  - 静かには落ちないと報告されている（`annHits.length < kPrime && annHits.length <
    eligible` で `Omission { kind: 'ann_unreached' }` が立つ）が、`countKind: 'unknown'`
    なので「0/96 だった」ことは呼び出し側には伝わらない。

  **⭐ 上記のうち、`ann_unreached` の発火条件と `countKind: 'unknown'` の部分は、
  本 ADR の作業者がコードを読んで確認した**（伝聞ではない）。
  `packages/core/src/recall-runtime.ts:632-644` に、
  `candidateGenerationExecuted && kPrime > 0 && annHits.length < kPrime &&
  annHits.length < eligible` の条件で `omitted.push({ kind: "ann_unreached",
  countKind: "unknown" })` が立つコードがある。`eligible` は
  `aggregate.totalInScope - notIndexedTotal`（段5の集約に依存）であり、
  `countKind` は常にリテラル `"unknown"` で、`period` によって取りこぼした件数
  （上の例で言えば 96 件）を運ぶ欄はどこにも無い。⟹ 呼び出し側が受け取る `omitted` は
  「近似索引がこの scope に届かなかった」という定性的な事実だけで、
  「0件のはずが実は96件在った」という定量的な事実は伝わらない、という報告の記述は、
  コード上正確である。

- **⭐ この PR で一次実測した分（伝聞ではない）**:

  上の伝聞を放置せず、**この作業環境に PostgreSQL 17.11 + pgvector 0.8.6（CI が使う
  `pgvector/pgvector:pg17` と同じ版）を立てて、主張の中心部分を測り直した。**
  スキーマは手書きせず `packages/postgres` の移行スクリプト（0001/0002/0003）で作った。
  seed は 100,000行・256次元、`setseed(0.20260908)` で再現可能。`occurred_at` は
  **20% を NULL にして `COALESCE(occurred_at, recorded_at)` が実際に効く形**にした。
  `status` は active 69,929 / contested 10,074 / archived 9,935 / forgotten 10,062。
  `k' = 40`（`recall-runtime.ts:222-224` と `recall.ts:579,582` の
  `DEFAULT_RECALL_LIMIT = 10` × `DEFAULT_OVER_FETCH_FACTOR = 4` から読んだ値）。

  比べたのは3つ。**A** = 押し下げ無し（段1で period を絞らず、後段でだけ捨てる＝本 ADR 以前の挙動）、
  **B** = 押し下げ有り・3列式索引無し、**C** = 押し下げ有り・3列式索引有り（＝本 ADR が入れる形）。

  | 窓 | 実在数（厳密 `count(*)`） | A（押し下げ無し） | B（押し下げ・索引無し） | C（押し下げ・索引有り） |
  |---|---:|---:|---:|---:|
  | 0.1% | 87 | **0** | 40 | 40 |
  | 1% | 767 | **0** | 40 | 40 |
  | 10% | 8,095 | 4 | 40 | **31** |
  | 50% | 40,060 | 16 | 40 | 40 |

  **⭐ 再現したこと**: **狭い窓の取りこぼしは実在する。** 0.1%窓で 87件が実在するのに
  A は **0件** を返し、1%窓では 767件が実在するのに A は **0件** を返す。
  **押し下げを入れると `LIMIT` 一杯（40件）まで回復する。** 前任者が報告した
  「96件実在するのに0件、押し下げで40件」と**数字は違うが（seed が別物なので当然）、
  現象は同じものが出た。** 狭い窓では C は `idx_memories_period_ann_stage` を実際に使い
  （`Index Cond` に `tenant_id`・`status`・`COALESCE(...)` の3条件すべてが入る）、
  厳密に上位40件を取っていた。**本 ADR の押し下げが塞ぐ穴は、実在する。**

  **🔴 再現しなかったこと（前提が崩れた点）**: 前任者は「広い窓では現状と**構造的に同一プラン**に
  なり、**返却数も一致**（10%: 7 vs 7、50%: 21 vs 21）」と報告していたが、**この seed では
  成立しなかった。** 10%窓で C は A（4件）を大きく上回る 31件を返しており、**同一でも一致でも
  ない**（C のほうが良い）。⟹ **「退行なしの片側改善」という結論の向きは変わらない
  ——4窓すべてで C > A である**が、**その理由は「同一プランだから」ではなかった。**

  **🔴 そして、この再現で新しく見つかった限界（前任者の報告にも無かった）**:
  **3列式索引は「在れば必ず使われる」わけではない。** 10%窓では、プランナが
  `idx_memories_period_ann_stage` ではなく **HNSW 経由の近似プランを選び**（見積りコストは
  HNSW 経由 513 に対し式索引経由 1303）、HNSW から 391行を取り出したが period + status を
  満たしたのは31行だけで、**`LIMIT 40` に届かないままスキャンが終わった**（同一クエリを
  3回繰り返しても常に31件。ゆらぎではない）。50%窓では同じ HNSW 経由プランでも
  40件ちょうど埋まったが、**それは候補の母数が大きいからであって、設計上の保証ではない。**
  ⟹ **押し下げは「狭い窓で0件になる」という最悪の形を確かに塞ぐが、
  「`k'` を必ず埋める」ことまでは保証しない。**この残余は
  **`hnsw.iterative_scan` の腕（別 ADR）が扱う領域そのものである**——本 ADR で腕を
  分けた判断（「決定」5.）が、結果として正しかったことの傍証でもある。
  **⚠ ただし本 ADR はこれを「`iterative_scan` を入れるべきだ」とは決めない。**

  **⚠ この一次実測が引き受けている単純化**:

  - **埋め込みは時間軸と一切相関しない一様乱数**である。現実のデータでは「近い時期の記憶は
    内容も似る」という相関がありうる。⟹ **上の取りこぼしの大きさは、相関ゼロという
    最悪寄りの条件での値である可能性が高い。相関を持たせた seed では測っていない。**
  - **埋め込みテーブルだけは手書き**した（`memory_embeddings_bench`）。本番では
    `registerEmbeddingSpace()`（`packages/postgres/src/vector-space.ts`）がテーブル名を
    決めて作るが、DDL は `vector-space.ts` の `CREATE TABLE` /
    `CREATE INDEX ... USING hnsw (embedding vector_cosine_ops)` を書き写した。
  - **クエリベクトルは1本のみ**（`setseed(0.777)`）。前任者の測定と同じ制約が残っている。
  - **時間（ms）の値は、同じサーバで別の測定が同時に走っている下での観測**なので、
    本 ADR では速さの根拠に使わない。**件数（正しさ）は同時実行の影響を受けないので、
    上の表は信頼できる。**
  - **プランの分岐点（何%の窓で HNSW 経由に切り替わるか）は特定していない。**
    `random_page_cost` / `effective_cache_size` を本番相当に寄せた場合に
    10%窓の選択が変わるかどうかも見ていない。

- **測っていないこと（本 ADR の要）**:

  - **実測は 100,000行・256次元の1点のみ。** オーナーは100万件級を前提に設計すると
    決めている（`docs/roadmap.md:249`、2026-09-06、マネージャー経由）。**この決定が
    前提とする規模の1桁下でしか測っていない。**
  - **1,536次元は未測定。** 実運用で使う OpenAI 埋め込み（`text-embedding-3-small` 相当）
    の次元数であり、256次元とは埋め込みテーブルの行幅が異なる——プラン選択・費用が
    変わりうることは ADR 0023 が別の軸（次元ではなく規模）について既に指摘している
    通りである。
  - **クエリベクトルは1本のみ**測定に使われたと報告されている。
  - **同時実行下では未測定。** 単発クエリの中央値のみ、という報告。
  - **広い窓（10%/50%）の絞りは効かないまま残る。** 本 ADR はこれを塞がない
    ——現状＋後段フィルタと同一プランになる、という報告そのものが「広い窓では
    索引が効かず、従来と同じ挙動になる」ことを意味する。
  - **式索引は段5（`aggregateScope`）には効かない。** これは実測で反証済みと
    報告されている——`period` の述語は段5の `WHERE` ではなく
    `count(*) FILTER (WHERE ...)` の中に在る（`packages/postgres/src/memory-store.ts:724`
    付近、`filteredPeriod: { count: sum("period_filtered"), ... }`）ため、索引の
    有無でプランが変わらない。索引在り 32.2ms / 索引無し 36.0ms、プランは完全に同一、
    という値も前任者からの引き写しであり、裏取りしていない。

- **🔴 根拠に使ってはいけないもの（明示的に排除する）**:

  1. **「厳密経路の費用はテナント行数に比例して伸びる」（ADR 0023 の当初の代償節）は、
     本 ADR の根拠に使わない。** ADR 0023 自身の追記
     （`0023-subject-filter-in-ann-stage.md:187-196`「この実測が言っていること、
     そして見立ての訂正」節）が、これを自分で撤回している。**本 ADR の作業者が
     現物を読んで確認した要約**: ADR 0023 の当初の代償節は「上のプランは埋め込み
     テーブル側を `Seq Scan` している……この経路の費用はテナントの行数に比例して
     伸びる」と書いていたが、これは 3,000行・100 subject という小さい規模での
     観測だった。同じ ADR の追記（10,000行・100,000行で subject フィルタを再測定）
     では `Seq Scan` は一度も出ず、`idx_memories_by_subject` → 埋め込み表の主キーの
     `Nested Loop` が選ばれ、所要時間はほぼ横ばい（1.1ms→0.9ms、1.2ms→0.9ms）だった。
     ADR 0023 自身が「当時観測した3,000行での `Seq Scan` は、表が小さいときの産物
     だった可能性が高い」と結論している。⟹ 「厳密経路は規模に比例して重くなる」を、
     本 ADR の穴（式索引を追加すべき理由）の根拠として引用しない。
  2. **ADR 0011 の数字とその機構を、そのまま `period` に転用しない。** ADR 0011 の
     実測値は `1.5 ms → 156 ms`である（本 ADR の作業者が `grep` で現物を確認済み。
     `0.9ms → 228ms` という数字は本 ADR の対象リポジトリのどこにも存在しない誤りである）。
     **さらに機構が違う**——ADR 0011 が測ったのは `count(*) OVER ()`、すなわち
     **窓関数の集約**が索引を殺す現象であり、`period` は単なる**フィルタ**
     （`WHERE` 句の等値/範囲条件）である。ADR 0011 自身が `docs/recall.md` の
     「フィルタ問題」と「スコア問題」を区別しており、`count(*) OVER ()` は後者に近い
     集約の問題、`period` の押し下げは前者のフィルタの問題である。**同じ形の劣化
     （索引が死んで100倍遅くなる）が `period` にも起きる、と結論しない。**
     測ってはいないが、機構が違う以上、ADR 0011 の数字を根拠として引用しない。

- **採らなかった案**:

  - **`hnsw.iterative_scan` を有効化する**: 採らない。上記「決定」5. の通り、
    棄却ではなく別の腕として切り出した——本 ADR が扱う「索引側でフィルタを支える」
    手と、`hnsw.iterative_scan` の「ANN の探索を深くする」手は独立した対処であり、
    同じ PR に混ぜると効果が切り分けられない。
  - **既存の `idx_memories_recall_gate`（`tenant_id, status, decay_floor_at`）を、
    `period` も含む形に作り直す**: 却下。この索引は
    [ADR 0011](./0011-no-window-count-in-ann-stage.md)が「Phase 2 で
    `decay_floor_at` を読み取りフィルタに使い始める際に索引の作り直しが要らないように」
    という理由で3列目に `decay_floor_at` を持たせて設計したものであり、既に別の
    将来の判断（Phase 2 の decay 読み取り）のために予約されている列を持つ。ここへ
    `period` を混ぜると、`decay_floor_at` を使い始める Phase 2 の変更と、`period` を
    使う本 ADR の変更が同じ索引の同じ列を取り合う形になり、片方の都合でもう片方の
    索引設計が壊れる余地を持ち込む。**追加だけで独立した索引を1本作るほうが、
    将来の変更同士を疎に保てる。**
  - **2列 `(tenant_id, COALESCE(occurred_at, recorded_at))` の索引**: 却下。
    段1のクエリは既に `status IN ('active', 'contested')` で絞っており
    （`docs/recall.md` §3 の段1クエリ骨格）、`status` を索引の2列目に含めない場合、
    `status` は索引スキャン後の `Filter` として効くだけで、索引の絞り込みそのものには
    寄与しない。2列目に `status` を挟むことで、索引の時点で `status` の等値条件まで
    畳み込める。列順は「等値条件を前に、範囲条件を最後に」という B-tree の一般則
    （`tenant_id` 等値 → `status` 等値 → `COALESCE(...)` 範囲）に従っている。
  - **段1に降ろさず、後段フィルタだけで済ませる現状維持**: 却下。ADR 0023 が
    `subjectId` について指摘し、ADR 0056 が `excludeProvenanceKinds` について繰り返した
    同じ構造の穴（狭い絞り × 大規模テナントでの窓の取りこぼし）が `period` にも
    残ったままになる。上の実測（伝聞、裏取りなし）が正しければ、狭い窓では
    96件中0件しか返らない、という規模の取りこぼしであり、現状維持の負債としては
    大きい。

- **移行（migration）についての判断**:

  `packages/postgres/migrations/0003_period_ann_stage_index.sql` は**素の
  `CREATE INDEX`**（`CONCURRENTLY` を付けない）である。**これは意図的であり、正しい。**

  理由（本 ADR の作業者が `packages/postgres/src/migrate.ts` の現物を読んで確認した）:
  各マイグレーションは `migrate.ts:340`（⚠ 執筆時点の行番号。2026-09-13 現在は
  **`:532`**）の `await client.query("BEGIN");` から
  `migrate.ts:349`（⚠ 同。2026-09-13 現在は **`:541`**。`ROLLBACK` は `:544`）の
  `await client.query("COMMIT");` までの間で実行される
  ——つまり**1マイグレーション＝1トランザクション**であり、`CREATE INDEX
  CONCURRENTLY` はトランザクションブロックの内側では実行できない（PostgreSQL の制約。
  `CONCURRENTLY` はトランザクションの外側でしか使えない）。⟹ この実行系を使う限り、
  `CONCURRENTLY` という選択肢はそもそも存在しない。既存の
  `0002_outbox_claim_lease_index.sql` も同じ理由で素の `CREATE INDEX` を使っており、
  本マイグレーションはその先例と整合している。

  **⚠ 上の `:340` / `:349` の補足について（一般形。この repo の ADR 全体に効く読み方）:
  ADR は書き換えない文書なので、コードを指す行番号は必ず古くなる。**実際この2つは、
  執筆当時は正しかったが、2026-09-13 時点の `migrate.ts`（全 554 行）では
  `:340` は別関数の doc コメント・`:349` は `handOverLegacyMigrationsTable` の宣言行を
  指している。**⛔ だからといって「ADR が間違っている」と読んではならない**——
  `BEGIN` と `COMMIT` は実在し、1マイグレーション＝1トランザクションのままで、
  **上の結論は1文字も崩れていない。壊れたのは根拠の*住所*だけである。**
  **⟹ 行番号を単独の出典にせず、シンボル名や逐語の文字列を併記すること**
  （ここでは `await client.query("BEGIN");` という逐語が併記されていたので、
  `grep -n 'client.query("BEGIN")'` で現在値を数え直せた）。

  **引き受ける負債として記録する**:

  1. **索引作成中は `memories` テーブルに `ACCESS EXCLUSIVE` ロックが掛かり、
     その間 `memories` への書き込み（`observe()` からの挿入・`reinforce`・
     `setEmbeddingStatus` 等すべて）が止まる。** 100万件級のテーブルでは、
     この停止時間が実運用上無視できない長さになりうる——具体的にどれだけ止まるかは
     測っていない。
  2. **`CONCURRENTLY` が使えないのは、実行系（`migrate.ts`）が各マイグレーションを
     トランザクションで包んでいるという設計上の制約による。** マイグレーションの
     内容側の選択ではない——`0003_period_ann_stage_index.sql` だけを直しても解決しない。

  **⚠ 非トランザクションの移行経路を別に作るかどうかは、本 ADR の範囲を超える、
  もっと大きい判断である。** 本 ADR はこれを「そうすべきだ」とは決めない
  ——「別の判断として残っている」とだけ書く。決めるとしたら、既存の全マイグレーションの
  実行モデル（advisory lock によるプロセス間排他、ADR 0017）との整合を含めて
  再設計が要り、1本の索引追加のために決めることではない。

- **⭐ 変異試験の結果**:

  **撃った環境と門**: `feat/period-in-ann-stage` の `9557285` に対して、
  **この器に立てた PostgreSQL 17.11 + pgvector 0.8.6（CI と同じ `pgvector/pgvector:pg17`）
  を `DATABASE_URL` に渡した状態**で撃った。⟹ **ADR 0056 の変異試験（DB 無しの手元で
  行われ、DB 段が走っていなかった）と違い、本 ADR の変異試験は postgres の適合テストまで
  実際に走っている。**ルートの test 門が `✔ DB テストも実行し、通りました。` と
  出力していることを実文言で確認した（ADR 0015 の「実行していません」ではない）。

  回した門: `pnpm run typecheck` / `lint` / `format:check` /
  `test` / `build`。**基準線**（DB 込み・全て緑）: root vitest 59 / core 301 /
  openai 18（+2 skip）/ testkit 153 / postgres 223 / examples/chat 107 = **861 passed + 2 skipped**。
  **下の各行で「走ったテスト数」が基準線と一致していることは、歯が走る前に木が壊れていない
  （変異が型検査や import を落として早期終了させていない）ことの証拠である。**
  各変異は1本ずつ当て、当てる前に `git diff` で**変異が実際に木に載ったことを確認**し、
  測り終えたら復元して `git status --porcelain` が空であることを確かめた。

  **⚠ 層を区別すること。**period の絞りは4箇所に在る:
  **FVS** = `FakeVectorStore`（core のテスト用擬似物）、
  **IMS** = `InMemoryVectorStore`（testkit）、
  **PG** = `PostgresVectorStore`（段1の SQL）、
  **RR段2** = `recall-runtime.ts` の後段再検査。

  **⚠⚠ この表は 2026-09-08 時点の測定である。**下の `🔴 緑（生き残る）` / `0本` は
  **当時本当に測った値であり、書き換えていない**——塗り替えると、なぜ PR #72 / #73 を
  書いたのかという根拠そのものが消えるからである（「元から歯が在った」ように読める
  文書になる）。**⟹ この2件は同じ日のうちに両方とも塞がった。**末尾の
  **「その後（2026-09-08）」節**を必ず併せて読むこと。

  | # | 当てた変異 | 層 | 結果 | 赤くなった歯 | 走った数 |
  |---|---|---|---|---|---|
  | 1 | `occurredAfter` の `>=` → `>` | FVS | 赤 | **既存の歯のみ**（`observe-occurred-at.test.ts`、ADR 0037/0039 由来） | 301（不変） |
  | 1 | 同上 | IMS | 赤 | **新規の歯**1本（本 PR の適合テスト） | 153（不変） |
  | 1 | 同上 | PG | 赤 | **新規の歯**1本（実 DB） | 223（不変） |
  | 1 | 同上 | RR段2 | 赤 | **既存の歯のみ**（同上） | 301（不変） |
  | 2 | `occurredBefore` の `<=` → `<` | FVS | **🔴 緑（生き残る）** | **0本** | 301（不変） |
  | 2 | 同上 | IMS | 赤 | **新規の歯**1本 | 153（不変） |
  | 2 | 同上 | PG | 赤 | **新規の歯**1本（実 DB） | 223（不変） |
  | 2 | 同上 | RR段2 | **🔴 緑（生き残る）** | **0本** | 301（不変） |
  | 3 | `COALESCE(occurred_at, recorded_at)` → `occurred_at` だけ | PG | 赤 | **新規の歯**1本（実 DB） | 223（不変） |
  | 3 | 同上 | IMS | 赤 | **新規の歯**1本 | 153（不変） |
  | 3 | 同上 | FVS | 赤 | **既存の歯のみ**（対照テスト、ADR 0037 由来） | 301（不変） |
  | 4a | 段1へ `occurredAfter`/`occurredBefore` を渡すのをやめる | RR段1 | 赤 | **新規の歯2本**（配線の歯＋本命の歯） | 301（2 failed、不変） |
  | 4b | adapter が受け取っても無視する | FVS | 赤 | **新規の歯**1本（本命の歯） | 301（1 failed、不変） |
  | 4b | 同上 | PG | 赤 | **新規の歯3本**（境界2本＋COALESCE 1本） | 223（3 failed、不変） |
  | 5 | 段2の後段再検査を丸ごと消す | RR段2 | **🔴🔴 緑（生き残る）** | **0本** | **861 全部不変・全部緑** |

  **⭐ 変異3が噛んだことの意味**: `COALESCE` を外しても赤くなるということは、
  **`occurred_at` が NULL の行を含むフィクスチャが実際に在る**ということである
  （在らなければこの変異は等価変異と区別が付かない）。段5の「フィクスチャの非対称」は
  境界系の歯すべてで確認した——境界ちょうどの行と1ms外の行を対で持っており、
  期待値が実装の既定値と偶然一致するような弱さは見つからなかった。

  **⭐ 変異4a が噛んだことの意味**: 「狭い窓で0件になる」を狙った本命の歯が、
  押し下げを外すと実際に `expected [] to have a length of 3 but got +0` で落ちた。
  ⟹ **一次実測（上）で測った現象を、歯が実際に捕まえている。**この歯のフィクスチャは
  crowd（距離0・期間外）×3 と target（距離>0・期間内）×3 という非対称に組まれており、
  押し下げの有無がそのまま表面化する設計になっていた。

- **🔴 生き残った変異と、その理由（段2は誰も見ていない）**:

  **⚠ どちらも等価変異ではない——プログラムの意味は変わっている。歯が無いだけである。**

  1. **`occurredBefore`（上限）の境界は、`packages/core` の層で1本も検査されていない。**
     `<=` を `<` に変えても、FVS でも RR段2 でも緑のまま通る。
     `observe-occurred-at.test.ts`（ADR 0039 由来の既存の歯）は `occurredAfter`
     しか見ておらず、`packages/core/src/__tests__` 配下に `occurredBefore` の
     境界一致を検査する歯は無い。**testkit / postgres の適合テストは
     `store.search()` を直接呼ぶので両端とも捕まえるが、core 自身は下限しか見ていない。**
     ⟹ **本 ADR が `VectorFilter` に足した2つのフィールドのうち、
     `occurredBefore` は adapter の層でしか守られていない。**

  2. **🔴🔴 段2（後段の period 再検査）を丸ごと削除しても、DB 込みのフルスイート
     861 テストが1本も赤くならない。** `typecheck` も緑（`effectiveTime` の
     変数ごと消えるので未使用変数にもならない）。
     **⟹ `vector-store.ts` のクラス doc と `recall-runtime.ts` のコメントが
     「段1と段2の多層防御」「二重に見えるが意図的」と明言している、その段2の側には、
     それを守る歯が1本も無い。**
     理由: 既存の歯も本 PR の新しい歯も `recall()` の公開 API を通すため、
     段1と段2の**両方**を経由してしまい、「段1がザルで段2だけが救う」状況を作れない。
     段1（FVS / IMS / PG のいずれも）が正しく period を適用している限り、
     **段2は到達不可能なコードである。**

  **⚠ これは ADR 0056 が「段1と段2が互いを庇っている」として記録した構造と同じものである**
  ——0056 は `excludeProvenanceKinds` について同じ形の生き残りを記録した。
  **本 ADR は `period` でも同じ穴が空いていることを確認した。本 PR ではこの2つの穴を
  塞がない**（歯を足すのは別の判断であり、段2を守る歯を書くには段1を意図的にザルにする
  仕掛けが要る）。**塞がないことを、測った上で記録する。**

  **⟹ ⚠ この判断（「本 PR ではこの2つの穴を塞がない」）は、同じ日のうちに
  PR #72 / #73 で覆した。上の文は当時の判断としてそのまま残す。**
  **🔑 覆した理由は「不可能だと思っていたが実は可能だった」ではない**——本 ADR 自身が
  「歯を足すのは別の判断であり」と書いているとおり、これは**範囲の選択**であって、
  **塞げないという測定ではなかった。**⟹ 詳細は末尾の「その後（2026-09-08）」節。

- **⚠ この変異試験が測っていない範囲**:

  - **段2の再検査は、本 PR が足したコードではない。** 差分はコメントの更新だけで、
    `effectiveTime` による `continue` の2行は ADR 0039 由来の既存実装である
    （変異試験の作業者が `git diff` で確認し、当初の見立てを訂正した）。
    ⟹ 上の生き残り2. は**本 PR が持ち込んだ欠陥ではなく、本 PR が可視化した既存の欠陥**である。
  - **等価変異の網羅的な探索はしていない。** 撃ったのは境界・区別・押し下げの除去・
    後段の除去という4系統だけである。
  - **`status` と `decayFloorAtAfter`（後段が見ないフィールド）には撃っていない。**
    それらは ADR 0034 / 0056 の範囲である。

- **これが覆るとしたら**:

  - **1,536次元・100万件級・同時実行下での実測**が行われ、狭い窓での改善が
    256次元・100,000行の実測と同じ傾向を示さなかったら、この索引の設計
    （列順・式）を見直す。
  - **索引作成の `ACCESS EXCLUSIVE` ロックが実運用で許容できない長さになる**ことが
    判明したら、非トランザクションの移行経路（`CONCURRENTLY` を使える実行系）を
    別途設計する、より大きな判断を起こす引き金になる。
  - **中間の窓でプランナが式索引を選ばず、`k'` を埋めきれない**——これは仮定ではなく
    **本 PR で実際に観測した**（10%窓で 31/40。上の「一次実測」節）。これが運用で問題として
    顕在化したら、`hnsw.iterative_scan` の腕、あるいは段5側の集約高速化を別 ADR で
    検討する。**本 ADR はこの残余を塞がない。**
  - **`period` の取りこぼしが `omitted` から読み取れないこと（`countKind: 'unknown'`
    のまま）が運用上困る**と分かったら、`ann_unreached` の情報量を増やす設計
    （ADR 0025/0026 の延長）を検討し直す。

- **確かめていないこと（まとめ）**:

  - **前任者が報告した具体的な数値（96件中0件・押し下げで40件・1.9ms vs 8.5ms・
    段5の 32.2ms vs 36.0ms）そのものは、いまも伝聞であり裏取りしていない。**
    本 PR が測り直したのは**主張の中心（狭い窓での取りこぼしと、押し下げによる解消）
    だけ**であり、seed が別物なので数値の直接比較はできない。
    **速さについては、本 PR の測定は同時実行の負荷下でしか取れていないため、
    「速くなる」ことの根拠には使えない**——本 ADR が根拠にしているのは
    **件数（正しさ）であって、速さではない。**
  - `ann_unreached` の発火条件・`countKind: 'unknown'` の記述のみ、コードを読んで
    確認済み（伝聞ではない）。
  - `migrate.ts` のトランザクション構造・`ACCESS EXCLUSIVE` ロックの発生自体は
    現物のコードと PostgreSQL の一般的な仕様から導いたものであり、実際に本番規模で
    ロック待ちがどれだけ発生するかは測っていない。
  - ADR 0039 が指摘した「実効時刻の定義が5箇所に増える」ことについて、
    `VectorFilter.occurredAfter`/`occurredBefore` の境界規則（両端包含）が
    他の4箇所と揃っていることは、本 PR の実装（`packages/postgres/src/vector-store.ts`
    の `>=`/`<=`）を読んで確認した。**境界の歯も実際にこの5箇所目を狙って書かれている
    ことは、本 ADR の作業者が `packages/testkit/src/vector-store-conformance.ts` の
    差分を読んで確認した**——境界ちょうど・`COALESCE` のフォールバック（`occurredAt`
    が `null` のとき `recordedAt` を使う）を検査する3本が追加され、postgres/in-memory
    両方の適合テストに配線されている（`conformance.postgres.test.ts`・
    `in-memory-fixtures.conformance.test.ts` の差分）。**⭐ この記述は本 PR の途中で古くなった**——
    その後この器に PostgreSQL 17.11 + pgvector 0.8.6 を立てたので、
    **postgres 側の歯は実際に実行して緑になることを確認済みであり、さらに変異を当てて
    赤くなることも確認した**（上の「変異試験の結果」節）。

## その後（2026-09-08）—— 上の表で生き残った2件は、両方とも同じ日のうちに塞がった

**⛔ 上の変異試験の表（`🔴 緑（生き残る）` / `0本`）は書き換えていない。**あれは
2026-09-08 時点で実際に測った値であり、塗り替えると **なぜ #72 / #73 を書いたのかという
根拠そのものが消える**（「元から歯が在った」ように読める文書になる）。
⟹ 元の測定はそのまま残し、その後を以下に継ぐ。

| 上の表の行 | 生き残っていたもの | 塞いだ PR | 置かれた歯 |
|---|---|---|---|
| 変異2（FVS）・変異2（RR段2）＝**生き残り1件目** | `occurredBefore`（上限）の境界が `packages/core` の層で無検査（`<=` → `<` にしても緑） | **#72**（マージ 2026-09-08T10:51:36Z） | `packages/core/src/__tests__/observe-occurred-at.test.ts` と `fake-vector-store-filter.test.ts` に3つの `it` |
| 変異5（RR段2）＝**生き残り2件目** | 段2の後段 `period` 再検査を丸ごと消しても 861 本が1本も赤くならない | **#73**（マージ 2026-09-08T13:23:51Z） | `packages/core/src/__tests__/recall-period-filter.test.ts` の `PeriodStrippingVectorStore` を使う describe |

**#73 が採った処方は、[ADR 0056](./0056-exclude-provenance-kinds-in-ann-stage.md) が
既に書き残していたものである**——「段1がわざと絞らない adapter（あるいは段1の filter から
当該フィールドを意図的に外したテスト専用の `VectorStore`）を使って段2だけを検査する歯」。
`FakeVectorStore` を薄く包み、`search()` に渡ってきた `opts.filter` から
`occurredAfter`/`occurredBefore` **だけ**を剥がして委譲する（他のフィールドは素通し
——period 以外まで段1でザルにすると、この歯が「period だけを測っている」と言えなくなる）。
**⛔ 本番コードは1文字も弱めていない。**ラッパは test ファイルの中に閉じている。

### ⭐ 「歯を置いた」と「歯が噛む」は別である —— 2026-09-13 に測り直した

**#73 が歯を置いたことと、その歯が実際に噛むことは別の主張である。**⟹ 後者を、
`main`（`5f794f1`）に対して変異を当てて確かめた。

**測り方（名乗り）**: **変異はすべて手で当てた。**`.claude/skills/mutation-testing/` の
ハーネスは使っていない ⟹ **足場対照（scaffold control）の差し引きは無い**（下の数は
そのまま素の結果である）。各変異は1本ずつ当て、**当てた直後に `git diff` で木に載った
ことを確認してから**テストを走らせた——**当たっていない変異は必ず緑になるので、確認を
省くと「変異を入れたのに緑だった」と誤報告する。**測り終えたら復元し、
`git status --porcelain` が空であることを確認した。

走らせたコマンドの逐語: `pnpm --filter @mnemora/core run test`。
**基準線: `Test Files 37 passed (37)` / `Tests 526 passed (526)`（全緑）。**
**⚠ この 526 は 2026-09-13 時点の `packages/core` 単独の数であり、上の表の 301 や
861（DB 込みの全体）とは別物である ⟹ ⛔ 引き算して比べてはならない。**

| # | 当てた変異 | 段2専用の歯（#73） | 赤くなった歯 | 走った数 |
|---|---|---|---|---|
| **M1** | 段2の `continue` 2行を**丸ごと削除**（＝上の表の変異5と同じもの） | **🔴 赤** | `recall-period-filter.test.ts:299`（段2専用の歯そのもの） | 1 failed / 526（不変） |
| **M2** | 段2の `effectiveTime < scope.occurredAfter` → `<=` | 緑 | `observe-occurred-at.test.ts:220`（#72 が足した下限の境界の歯） | 1 failed / 526（不変） |
| **M3** | 段2の `effectiveTime > scope.occurredBefore` → `>=` | 緑 | `observe-occurred-at.test.ts:287`（#72 が足した上限の境界の歯） | 1 failed / 526（不変） |
| **M4（⭐ 対照）** | 段2は触らず、**段1**（`FakeVectorStore` の period 適用）を丸ごと削除 | **緑のまま** | `fake-vector-store-filter.test.ts:150` と `recall-period-filter.test.ts:190`（段1を測る歯2本） | 2 failed / 526（不変） |
| **M5（⭐ 赤くなってはいけない変異）** | 段2の2行の**順序を入れ替える**（どちらも副作用の無い `continue` ⟹ 真の等価変異） | **緑** | **0本** | **526 passed（全緑・不変）** |

M1 の失敗の実文言:
`AssertionError: expected [ 'future-outside-period', …(3) ] to not include 'old-outside-period'`
（`recall-period-filter.test.ts:299`）

**走った数がすべて基準線 526 と一致している**ことは、歯が走る前に木が壊れていない
（変異が型検査や import を落として早期終了させていない）ことの証拠である。

**🔑 M1 が赤・M4 が緑**という組み合わせが、この歯について言いたいことのすべてである:

- **M1（段2を消すと赤）** ⟹ **歯は段2に届いている。**上の表の変異5——861 本が1本も
  赤くならなかったあの生き残り——は、**いま赤くなる。**
- **M4（段1だけを壊しても緑）** ⟹ **この歯は段2「だけ」を測っている。**段1の実装の
  有無とは独立している（`PeriodStrippingVectorStore` がどのみち period を剥がすため）。
  ⟹ **ADR 0056 が「段1と段2が互いを庇っている」と書いた構造が、この歯の中では解けている。**
- **M5（等価変異で緑）** ⟹ **この歯は「性質」を測っており、「式の書き方」を測っていない。**
  ふるまいを変えない変異で赤くなる歯は、リファクタリングのたびに嘘の赤を出す。

**⚠ M2 / M3 が段2専用の歯に対して緑だったことを「歯が無い」と読んではならない。**
段2専用の歯のフィクスチャは**境界ちょうどを持たない**（期間外の候補は 2025-01-01 と
2026-05-15、境界は 2026-01-01 と 2026-05-01）⟹ **境界を1つずらす変異には設計上反応しない。**
**境界のほうは #72 が足した別の歯（`observe-occurred-at.test.ts`）が測っており、
M2/M3 では実際にそちらが赤くなった。**⟹ 2本の歯が別々の性質を留めていて、
どちらも当てすぎていない。

### ⚠ この再測定が測っていない範囲

- **「段1と段2を*同時に*壊す変異」は撃っていない。**⟹ ADR 0056 が指す
  「互いに庇い合う」構図そのものの検証には踏み込んでいない。
- **DB 段は走らせていない**（`packages/core` 単独のスイートだけを回した）。
  上の表の 2026-09-08 の測定は DB 込みで撃たれており、**そこは条件が違う。**
- **🔴 `excludeProvenanceKinds` についての同じ形の生き残り（ADR 0056 の変異5・変異6）は、
  2026-09-13 時点でまだ塞がっていない。**
  `packages/core/src/__tests__/recall-exclude-provenance-filter.test.ts` に段2専用の歯は
  無い（同日 `grep` で確認。段1を測る歯だけが在る）。
  **⟹ #73 の `PeriodStrippingVectorStore` がそのまま雛形になる。**

## 訂正 (2026-09-08)

本 ADR が記録した速さ（`1.9ms vs 8.5ms` 等）は、いずれも **`ANALYZE` を実行した
後**に測定されたものである。ところが `0003_period_ann_stage_index.sql` 自体は
`ANALYZE` を一切実行しない——つまり、本 ADR の測定が成立していた条件は、当時出荷した
マイグレーション自体が作ったものではなく、測定環境側で別途 `ANALYZE` が走っていた
ことによる。この条件を実際に作るマイグレーション（`0005_analyze_memories.sql`）は
PR #69 で足された。実測の数字と、`tenant_id` 先頭の索引・`ANALYZE` の要否に関する
一般則は [ADR 0062](./0062-contested-with-id-fk-index.md) を参照。

**⚠ PR #69 の期間中に PostgreSQL 17.11 で再測定したところ、本 ADR の元の数値は
再現せず（`ANALYZE` 前 35.0/39.8/42.4ms → `ANALYZE` 後 4.6/4.5/5.6ms、という別の
数字が測れた——詳細・裏取りは ADR 0062 参照）。
