# ADR 0280: `compare` の `omitted` の `stage` 集合が動いたら、申告を要求する門を置く（Issue #403）

- **状態**: 採用 (2026-09-23。[ADR 0283](./0283-adopt-merged-adrs-whose-decision-is-on-main.md) で担い手が「提案」から倒した——オーナー本人の判定ではない)
- **日付**: 2026-09-23

> **⚠ この ADR は、自動化された担い手（クローンのマネージャーのセッション）のものである。**
> **⛔ オーナー本人の判定ではない**（[ADR 0220](./0220-issue-comment-author-does-not-distinguish-owner-from-agent.md)）。
> クローンの署名は repo 上では `takecchi` になり、**オーナー本人と区別が付かない。**
> ⟹ **この ADR を「オーナーが決めた」と読まないこと。**

**⚠ 各主張の出所を分ける。**

- **【実測】** — この書き手が自分の手で走らせて確かめた。
- **【現物】** — repo の中のコード・文書を読んで確かめた。
- **【受】** — 報告として受け取り、再導出していない（出所を明記する）。

---

## 問い

**[Issue #403](https://github.com/takecchi/mnemora/issues/403) が報告した形**【現物】:

`examples/chat/compare-baseline.json` の `omitted` 欄が `main` 上で陳腐化しており、
`example-chat` ジョブの Job Summary には**相違が出続けるのに、CI は緑のままである。**

⛔ **これは見落としではなく、設計どおりである**——⭐門（`scripts/compare-summary-lib.mjs` の
`GATE_FIELDS`）は `mnemoraShareOfNaiveChars` と `factStatementSurvived` の2欄しか見ない
（[ADR 0133](./0133-compare-baseline-and-gate.md)）。`omitted` は `DIFF_FIELDS` に在るので
**Job Summary には出る。**

⟹ 🔴 **`omitted` は「誰も見ていない欄」ではなく、「出ているが、誰も直す義務を負わない欄」である。**
⟹ **誰も直す義務を負わないので、腐る。**

## 決定 —— **落とす門にはしない。「申告を強制する門」として置く**

**`omitted` の `kind` と `stage` の組の*集合*が基準値から動いたら、その PR 本文に
「意図した仕様変更である」という*申告*を1行要求する。申告が在れば通し、無ければ落とす。**

申告の形（`scripts/compare-omitted-stage-declaration-lib.mjs` の `DECLARATION_PATTERN` が正本。
**ここに正規表現を写さない**——[AGENTS.md](../../AGENTS.md)「⚠ 数を、道具と生成物に焼き込まない」と同じ線）:

```
Compare-Omitted-Stage: <なぜ動いたのか。意図した仕様変更なら、その出所（ADR 番号など）>
```

⛔ **印だけ置いて理由が空の行は、申告として数えない。**

### 何を見て、何を見ないか

| | |
|---|---|
| ⭕ 見る | 各行の `omitted` の **`kind`+`stage` の組の集合**（重複は潰す） |
| ⛔ 見ない | **`count` / `countKind` の値**。件数は run ごとに揺れうる側であり、#403 の測定もそこは「揺れなかった」以上を主張していない【受: #403 のコメント（2026-09-21）】 |
| ⛔ 見ない | **申告の中身が本当か。**「理由が空でない」ことしか見ない |
| ⚠ 判定しない | **PR 本文が無いとき**（`main` への push）。⟹ `skipped` を出して緑で通る。⛔ **「動いていない」ではない**——CLI がそう名乗る |
| 🔴 判定不能 | **実測と基準値の `turnCount` 集合が食い違うとき** ⟹ **exit 2**。⛔ 申告が在っても黙らせられない（[Issue #477](https://github.com/takecchi/mnemora/issues/477) が `compare-summary` に入れたのと同じ線） |

## なぜこの形か

- ⛔ **`stage` 集合の一致を `GATE_FIELDS` に足す（＝落とす門にする）のは採らない。**
  `AGENTS.md`「⚠ **偽陽性率に上限を置けない検査は門にしない**」に正面から当たる
  ——`stage` 集合が動く理由には「退行」と「意図した仕様変更」の両方が在り、
  **機械にはその2つを区別できない。**
  ⚠ ADR 0133 自身が「全欄の厳密一致を門にする案」を、同じ理由で既に却下している【現物】。
- ⛔ **かといって「見ない」も採らない。**#403 が報告したのは、**その空白期間が実在した**ことである。
- ⟹ ⭐ **偽陽性の代償を「PR が赤になる」から「一行書く」へ落とす。**
  ⟹ **偽陽性率に上限を置けなくても、門にできる。**
- ⭐ これは `AGENTS.md`「⚠ **機械には「検出」まで — 確定と書き込みは人に残す**」の適用でもある
  ——**機械は検出して赤くするところまでで止まり、基準値を1バイトも書き換えない。**
  ⟹ 同 AGENTS.md の線（repo の中／GitHub 側の取り消しにくい面）でいえば、
  この門は**何も書き込まない**側に留まっている。

## 測ったこと【実測 2026-09-23】

- **歯を置き、変異試験で噛むことを示した**（`scripts/__tests__/compare-omitted-stage-declaration.test.mjs`、24件）:
  - **変異1**: `omittedEntryKey` から `stage` を落とす（＝ ADR 0188 が足した `stage` の出現を見逃す）
    ⟹ **4件が赤**
  - **変異2**: 申告の正規表現を緩め、理由が空でも通す ⟹ **1件が赤**
  - **どちらも戻すと 24件すべて緑に戻る**
- **陽性対照を歯に埋めた** —— #403 が報告した食い違い（基準値 `9fcd47c` vs ADR 0188 着地後の実測、
  5行）を fixture として持たせ、**この門がその5行を実際に捕まえること**を固定した。
  ⚠ **fixture の値は歯が持つ**——`main` の基準値ファイルから読んでいない。
  読むと `main` が動くたびに歯の意味が変わる
  （⭐ これは AGENTS.md の「⛔ 対象外 —— 実測して repo にコミットした基準値」に当たる:
  **どこかの正本の*写し*ではなく、それ自体が*測った記録*である**）。
- **`scripts/__tests__/` の全86ファイル・1564件が緑**（`ci.yml` を parse する既存の歯を含む
  ⟹ 足した workflow ステップの YAML も妥当である）。

### 🔴 踏んだ穴 —— `ci.yml` の構文エラーは、手元の歯が1本も捕まえない【実測 2026-09-23】

**この段を足した最初の push で、`ci.yml` が YAML として壊れた。**
段の名前を `'omitted' の …` と書いたため、**YAML が先頭のシングルクォートを引用符と解釈した。**
⟹ **workflow がそもそも起動せず、`jobs` が0件のまま `conclusion: failure` になった**
（[PR #625](https://github.com/takecchi/mnemora/pull/625) の最初の run）。

🔴 **そのとき、手元の `scripts/__tests__/` の 1564件はすべて緑だった。**
既存の `ci-yml-*-wiring` の歯は、逐語「**YAML は構造として解析していない（文字列で見ている）**」と
自ら断っており、**構文エラーは射程外**である【現物: `scripts/__tests__/ci-yml-compare-wiring.test.mjs`】。

⟹ ⭐ **配線の歯を1本足した**（`scripts/__tests__/ci-yml-compare-omitted-stage-declaration-wiring.test.mjs`、6件）。
変異試験: `--baseline` のパスを壊す ⟹ 1件赤／`PR_BODY` の env を落とす ⟹ 1件赤／
段ごと消す ⟹ 5件赤。**どれも戻すと6件緑に戻る。**
⛔ **ただしこの歯も YAML パーサを持たない**（下の「引き受けた負債」5番）。

⚠ **もう1つ踏んだ**: 段の名前を引用符で囲まずに `（Issue #403）` と書くと、
**`#` から先が YAML のコメントになり、名前が「（Issue」で切れる。**
⟹ 名前を二重引用符で囲んだ。**これは壊れた形ではないので、CI は緑のまま通っていた。**

### 🔴 外れた予測 —— **この PR 自身は、この門に噛まれなかった**【実測 2026-09-23】

**この ADR の書き手は、起票の時点で「この PR 自身が門に噛まれるはずだ」と予測し、
PR 本文に申告を1行入れて出した。**⛔ **外れた。**

**CI の出力は逐語**【実測、[PR #625](https://github.com/takecchi/mnemora/pull/625) の `example-chat` ジョブ】:

> `✔ \`omitted\` の stage 集合は基準値から動いていません。`

**理由**: `examples/chat/compare-baseline.json` の `provenance` が逐語で
「**2026-09-17**、`ADR 0231` の PR 自身の CI（run `35224407070`）が生成した artifact から複製」
と名乗っている【現物】⟹ **#403 が報告した具体的な腐り（基準値 `9fcd47c` 由来の5行）は、
その時点で既に直っていた。**

🔴 **そして、この事実は #403 のコメント（2026-09-21）に逐語で書かれていた**
——「(い) `e94b783`（現行の基準値）vs 現在の実測 | **0 / 12**」。
⟹ ⛔ **書き手はそれを読んでいたのに、逆を予測した。**記録として残す。

⭐ **これは、この ADR の決定を否定しない。**#403 が報告した問題は**2層**在る:

| | いまの状態 |
|---|---|
| **具体的な腐り**（ADR 0188 由来の5行） | ⭕ **既に直っている**（2026-09-17、ADR 0231 の PR が CI artifact から更新） |
| 🔴 **構造**（判定に使わない欄は、誰も直す義務を負わないのでまた腐る） | ⛔ **そのまま残っている** |

⟹ **この門が対象にしているのは後者である**（「引き受けた負債」2番に書いたとおり、
**この門が作るのは「次から腐らない」だけである**）。
⚠ **だから「噛まなかった」は、この門が効かないことの証拠ではない**
——**いま基準値と実測が一致している、という意味である。**
⛔ **ただし「効く」ことの証拠でもない。**この門が本番で噛んだ実例は、まだ1件も無い
——噛むことの証拠は、いまのところ**歯の中の陽性対照と変異試験だけ**である。

### 🔴 踏んだ穴2 —— **「書き方の例」が、申告として通っていた**【実測 2026-09-23】

**この門を入れた PR 自身の本文が、使い方を示すために囲み付きコード片の中へ
`Compare-Omitted-Stage: <なぜ動いたのか…>` と書いていた。**
⛔ **正規表現はコード片を知らないので、その*例示*が申告に一致していた。**

⟹ 🔴 **「説明を丁寧に書くほど門が緩む」という向きである。**
⟹ **`findDeclaration` が、囲み付きコード片を落としてから探すようにした。**
**歯を2件足し、変異試験で噛むことを示した**（`stripFencedCode` を素通しにする ⟹ **2件が赤**、戻すと **26件緑**）。

⚠ **この穴は、CI からは見えなかった** —— この PR では `stage` 集合が動いていないので、
**申告が在ろうと無かろうと緑**だったからである。⟹ **見つけたのは、本文から不要な申告行を
消そうとして「1箇所残る」と気づいたときである。**

## 検討して採らなかった案

1. **(A) `omitted` を `GATE_FIELDS` に足して落とす門にする。**⛔ 却下——上記「なぜこの形か」1番。
   ⚠ **#403 のコメントが「A′（`stage` 集合だけを門に加え、`count` は加えない）」として測っており、
   過去の意図した変更で 5/12 行が赤くなると出ている**【受: #403 のコメント（2026-09-21）】
   ⟹ **偽陽性の実在が示されている。**
2. **(B) 基準値ファイルから `omitted` 欄ごと落とす。**⛔ 却下——`stage: "association"` のような
   「連想枠が何件捨てたか」の記録が基準値から消える ⟹ 次に同種の穴が開いたとき、
   基準値からは検知できなくなる（#403 本文が挙げた代償）。
3. **(C) いま基準値を更新するだけ。**⛔ 却下——**また腐る。**判定に使わない欄は、次に形が変わったときも
   誰も直す義務を負わない ⟹ **同じ ISSUE がもう一度立つ。**
4. **機械が基準値を自動更新する。**⛔ 却下——`AGENTS.md`「機械には検出まで」に反する。
   「意図した変更か退行か」を機械が判定できない以上、自動更新は**退行を黙って飲み込む。**
5. **`count` も申告の対象にする。**⛔ 却下——件数は run ごとに揺れうる側であり、
   申告を要求する回数が増えて**申告そのものが儀式化する。**

## 引き受けた負債

1. 🔴 **この門は、この CI 実行を起こした push の時点の PR 本文しか見ない。**
   ⟹ **最後の push の「後」に `gh pr edit` で申告を足しても効かない**
   （`ci.yml` の `pull_request` トリガーに `edited` を足していない）。
   ⚠ **`scripts/check-pr-adr-reference.mjs` が同じ穴を持ち、同じ断りを書いている**【現物】
   ——**この ADR は穴を塞いでおらず、既存の作法（申告を書いてから push する）に乗っているだけである。**
   ⚠ **この順序を逆にして赤を1回増やした実例が在る**【実測 2026-09-23、[PR #620](https://github.com/takecchi/mnemora/pull/620)】。
2. **`omitted` の陳腐化そのものは、この PR では直していない。**この門は「**次から腐らない**」を作るだけである。
   ⟹ **いま基準値に載っている古い `omitted` は、古いままである。**
   ⛔ **更新は CI artifact からのみ行う**（[ADR 0121](./0121-bench-baselines-from-ci-artifacts.md) /
   [ADR 0231](./0231-compare-baseline-omitted-measured-update-and-freshness.md)）。この作業環境には `DATABASE_URL` が無く、
   **`compare` ベンチを一度も走らせていない**【実測】。
3. **申告の中身の妥当性は、誰も検査しない。**「一行書けば通る」形である
   ⟹ **申告が儀式になる可能性が在る。**⚠ そうなったかどうかは、この ADR では測れない。
4. 🔴 **`ci.yml` の YAML 構文の妥当性を、手元では検査できない。**
   上の「踏んだ穴」のとおり、既存の `ci-yml-*-wiring` の歯も、この ADR が足した歯も、
   **文字列で見ているだけでパーサを持たない。**
   ⛔ **YAML パーサを repo の依存に足すのは、この ADR では採らない**
   ——`docs/autonomy.md` §3 の ⛔ に「**依存の追加方針の変更**」が挙がっており、
   **オーナーの判断**だからである（ADR 0014 / ADR 0061）。
   ⟹ **構文の妥当性は、いまも CI に出してみるまで分からない。**
   ⚠ 今回は `npx js-yaml`（repo の依存にしない、その場限りの実行）で検算した【実測】。
5. **`example-chat` ジョブの中に置いた**（新しい required context を作っていない）
   ⟹ **`compare` が走らない状況ではこの門も走らない。**

## これが覆るとしたら何が起きたとき

- **申告が儀式化したとき**（内容の無い申告が積み重なる）⟹ 申告の形を強める、または (A) を採り直す。
- **`stage` 集合が「意図した仕様変更」以外の理由で頻繁に動くと分かったとき**
  ⟹ この門は偽陽性の山になる ⟹ 見る対象を狭めるか、撤回する。
- **`omitted` の構造そのものが変わったとき**（`kind`/`stage` 以外の軸が増える）
  ⟹ `omittedEntryKey` の定義を見直す必要が出る。
- **`ci.yml` の `pull_request` トリガーに `edited` が足されたとき** ⟹ 負債1 が解消する。

## 確かめていないこと

1. **`compare` ベンチを一度も走らせていない**——この作業環境に `DATABASE_URL` が無い。
   ⟹ **この門が本物の `compare.json` に対してどう振る舞うかは、CI で初めて分かる。**
2. **#403 のコメントが測った数値（n=10 の run 間一致、5条件の順序・件数実験、A′ の 5/12）を
   再導出していない**【受】。この ADR が使ったのは、その**結論の形**だけである。
3. **申告の正規表現が、実在の PR 本文の書き方すべてに耐えるかは測っていない。**
   歯で固定したのは「理由あり／理由なし／引用・箇条書きの装飾つき／本文が文字列でない」の4形だけである。
4. **オーナー本人の確認は取っていない**（冒頭のとおり）。

---

## 🔴 追記1（2026-09-23、着地当日）—— **「`main` への push では `skipped`」は誤りだった**

**上の「何を見て、何を見ないか」の表は、逐語で次のように書いている**:

> ⚠ 判定しない | **PR 本文が無いとき**（`main` への push）。⟹ `skipped` を出して緑で通る。

🔴 **⛔ 現物はそうなっていなかった。**

**【実測 2026-09-23、この ADR がマージされた直後の `main` の CI】**
`main` の post-merge で、この門は **`skipped` ではなく `✔ omitted の stage 集合は基準値から動いていません。`**（＝ `no_change`）を出していた。

**理由**: **GitHub Actions は `push` でも `env: PR_BODY: ${{ github.event.pull_request.body }}` を、空文字列として渡す。**
⟹ 初版の判定は `typeof prBody === "string"` しか見ていなかったので、**空文字列は「本文が在る」側に落ちていた。**

🔴 **たまたま `stage` 集合が動いていなかったので緑だっただけである。**
⟹ ⛔ **もし動いていたら、`main` が「申告が無い」で赤くなり——そして `main` には申告を書く場所（PR 本文）が存在しない。**
⟹ 🔴 **満たしようのない門になっていた。**

### 直したもの

**「本文が在る」の判定を `hasPrBody` に切り出し、未設定だけでなく空・空白だけも「無い」として扱う。**

**【実測 2026-09-23】**

- 歯を3件足した（未設定 / 空文字列 / 空白だけ の3形が `skipped` になること）
- **変異試験**: `hasPrBody` を `typeof prBody === "string"` に戻す ⟹ **2件が赤**。戻すと **29件緑**
- `scripts/__tests__/` の全89ファイル・1606件が緑（別に skip 2件）。`lint` / `format:check` も緑

### ⛔ 本文は書き換えていない

**上の表の「⟹ `skipped` を出して緑で通る」は、そう*設計したつもりだった*という記録である**
（`docs/decisions/README.md`「採用済み ADR の本文は書き換えない。訂正が要るなら、その場に追記する」）。
⟹ **この追記が正である。**

### ⭐ 何が教訓か

**「門が緑だった」は「門が意図どおり動いた」ではない。**
この門は `main` で**緑を出していたが、出していた理由が設計と違った。**
⟹ ⛔ **緑の *理由* まで読まなければ、設計とのずれは見えない。**
⟹ ⭐ **見つけたのは、post-merge の CI ログで「この門が何と言ったか」を逐語で確かめたときである**
——**「post-merge が success だった」だけを見ていたら、見つからなかった。**

---

## 追記2（2026-09-23）—— **追記1 が引き受けた負債: 空本文の PR では、この門は黙る**

> **⚠ この追記は、自動化された担い手（クローンのマネージャーのセッション）のものである。**
> **⛔ オーナー本人の判定ではない**（[ADR 0220](./0220-issue-comment-author-does-not-distinguish-owner-from-agent.md)）。⚠ GitHub の actor は層を判別しないので、この追記を含む PR も `takecchi` の名前で載る。**名前で読まないこと。**

**追記1 は「満たしようのない門」を直した。⛔ だが、代わりに何を手放したかを書いていない。**⟹ **ここに書く。**⛔ **これは穴ではない。意図した取引である。**

### 🔴 引き受けた負債

**PR の本文が空・空白だけのとき、この門は `skipped` になる。**⟹ **`stage` 集合が動いていても赤くならない。**

⚠ **この門は required の中に在る**【現物】——`.github/workflows/ci.yml` で、この門を走らせる step は job `example-chat`（`name: examples/chat (本物の Postgres + pgvector、擬似 provider)`）の中に在り、**その名前は branch protection の required status checks の1つである。**

⟹ 🔴 **空本文の PR は、required な門を素通りできる。**

| 場合 | 追記1 の前 | 追記1 の後（いま） |
|---|---|---|
| `main` への push（`PR_BODY` が空文字列） | 🔴 評価される ⟹ `stage` が動けば**満たしようのない赤** | ⭕ `skipped` |
| **PR で、本文を空にした**（`stage` が動いた） | `undeclared` ＝ **赤**（門が効く） | ⚠ **`skipped` ＝ 緑**（門が黙る） |

### ⭐ なぜこの取引を選んだか

**`main` への push では `PR_BODY` が空文字列として渡る**（追記1 の【実測】）。⟹ **空を「在る」と扱うと、`main` で `stage` 集合が動いた瞬間に「申告が無い」で赤くなり、しかも `main` には申告を書く場所（PR 本文）が存在しない。**

⟹ ⭐ **「空＝無い」に畳む以外に、両方を満たす形が無い。**
⛔ **`main` か PR かを `github.event_name` で分岐させる形は、この ADR の判定（純関数）に GitHub Actions の文脈を持ち込むことになるため、ここでは採っていない**——⚠ **採らなかった案として記録するだけであり、覆せないという意味ではない。**

### ⭐ 到達可能性【実測 2026-09-23】

```
gh api --paginate "repos/takecchi/mnemora/pulls?state=all&per_page=100"
  → 全 445 本（100 × 4 + 45）
  → うち本文が空・空白だけのもの: 0 本
```

⟹ **原理的には通るが、この repo で一度も起きていない形である。**⛔ **「起きていない」を「起きない」と読まないこと**（`AGENTS.md`「⚠ 『出なかった』を、事象が無いことの証明にしない」）。

### ⭕ 黙って通るのではない —— **この取引を受け入れられる理由**

**CI は `⊘ PR 本文が無いので判定していません（\`PR_BODY\` が未設定・空・空白だけ）。⛔ 「動いていない」ではありません。` と出力で名乗る。**
⟹ ⛔ **黙って緑になるのではない。**⭐ **読めば「判定していない」と分かる形で通る。**

### ⛔ この追記が決めないこと

- **この負債を塞ぐかどうか。**⟹ 塞ぐなら「`main` か PR かを判定の外で切り分ける」形になり、上の「採らなかった案」を覆す判断が要る。
- **`stage` 集合が動いたのに申告が無いまま着地した実例が在るか**は掃いていない。⟹ **上の 0/445 は「本文が空だった PR」の数であって、「門を素通りした変更」の数ではない。**
